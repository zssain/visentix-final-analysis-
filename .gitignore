# Secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.pyc
*.egg-info/
dist/
build/
.venv/
venv/

# Node
node_modules/
.next/

# Local overrides
*.local

# Model / weights cache
.cache/
/models/
*.gguf
*.bin

# SEC EDGAR bulk download (local batch-import source, ~15GB — never commit)
/SEC EDGAR/
edgar_bulk/

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store
local_users.json

# Princeton-Leuven sector CSVs (research-license data — never commit)
/princeton_extract/

# Embedding backfill runtime artifacts (per-run; resumable state)
logs/embed_backfill.run.log
logs/embed_backfill.checkpoint.json

# Live schema dumps (local insurance before migration applies) — never commit
db/schema_dumps/
logs/saas_crawl.log
