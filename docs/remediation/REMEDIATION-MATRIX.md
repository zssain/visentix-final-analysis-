# Visentix Remediation Matrix — running source of truth

**Created:** 2026-08-04 (Prompt 0) · **Baseline:** `main @ 8c4ed92` · **Source of findings:** `~/Downloads/Visentix-Codebase-Review-Report-v1.0.pdf` (Full Issue Register §12) — see `00-BASELINE.md` §5 for why this PDF, not the coarser repo markdown, is authoritative.

**In this prompt every row is a verification verdict only — NO fixing was done.** Later phases update the **Status** column in place.

### Status vocabulary
`NOT STARTED · CONFIRMED · ALREADY FIXED · PARTIALLY FIXED · IMPLEMENTED · TESTED · BLOCKED · NOT REPRODUCIBLE`
At Prompt-0 close, every row is one of: **CONFIRMED · PARTIALLY FIXED · BLOCKED** (nothing was NOT REPRODUCIBLE or ALREADY FIXED).

### Column key
Sev = report severity · RptSt = report verification status (VER=verified firsthand / REP=reported) · Ph = report phase · B/F/D = touches Backend / Frontend / DB.

### Headline
**37 findings verified** (the register lists 37 rows; the PDF exec-summary miscounts HIGH as 9 vs 10). **35 CONFIRMED exactly as described · 1 PARTIALLY FIXED (CRED-001) · 1 CONFIRMED-with-measurement-BLOCKED (EVAL-001).** No finding was already-fixed or non-reproducible. Verified firsthand against `main @ 8c4ed92`; **GRD-003 verified by executing the guardrail** (results below).

---

## A. Critical

| ID | Sev | RptSt | Ph | Current-code verification | Root cause | Affected files (current path:line) | B | F | D | Test required | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|
| SEC-001 | CRIT | VER | 1 | `list_findings` builds `risk_finding?select=…&order=score.desc&limit=200` with **no `organization_id` filter** for `customer`; sibling `dashboard_stats` builds `org_clause = &organization_id=eq.{org}`. Reproduces exactly. | Org-scoping pattern omitted on one route; no cross-tenant contract test; service role bypasses RLS so no backstop. | `app/routers/findings.py:27-32` (leak) vs `:113-114,153-157` (scoped sibling); `app/db.py:11-20` | Y | N | Y | Cross-tenant contract test iterating every tenant-scoped route → 403/empty for other-org ids | **TESTED (Phase 1)** |

## B. High

| ID | Sev | RptSt | Ph | Current-code verification | Root cause | Affected files (current path:line) | B | F | D | Test required | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|
| GRD-002 | HIGH | VER | 1 | `"guardrail": exec_meta.get("guardrail", "passed")` defaults to "passed"; provenance prose asserts the text "passed the banned-term guardrail"; nothing populates a real result. | Permissive default standing in for a signal the assembly path never produces. | `app/services/report/explain.py:497` (also `:515`), prose `:485-495` | Y | N | N | Assert honest-absence ("not_recorded") when no real guardrail result exists | **TESTED (Phase 1)** |
| GRD-003 | HIGH | VER-exec | 1 | **Executed against the real module:** (a) "The company's practice violates the vendor's terms." → **BYPASS**; (b) "It's a case where the policy violates what we'd expect." → **BYPASS**; (c) "…clause 'violates' expected norms." → **BYPASS**; (d) "This clearly violates the rule." → **CAUGHT**. Matches report. | Over-broad `'[^']*'` alternation exempts any span between two apostrophes (contractions) as a "source excerpt"; the harmful case is untested. | `app/services/guardrail.py:24-29` (QUOTE_PATTERN), `:98-124`; test `tests/test_guardrail.py:93-96` proves single-quotes with a *source* excerpt (conflation) | Y | N | N | Add a/b/c as regression fixtures + keep (d) caught; remove single-quote alternation | **TESTED (Phase 1)** |
| GRD-001 | HIGH | VER | 2 | Zero `narrative`/`enforce`/`guardrail` refs in `reports.py`; exec summary built as f-string (`:499`), `"prose": rec["body_template"]` emitted verbatim (`:535`). `narrative.py` imported only by `rewrite.py:21`. | `generate_narrative()` never wired into report assembly; assembly re-implements prose inline. | `app/routers/reports.py:499-543`; `app/services/narrative.py:20,280,296`; `app/services/rewrite.py:21` | Y | N | N | Seed a banned term into a `body_template` row → assembly fails closed | **TESTED (Phase 2)** |
| FUNC-001 | HIGH | VER | 2 | `clauseText = PLACEHOLDER_CLAUSE` (never from `selected`); hardcoded `C-118`, `SH-002`, Exposure 72.4/VCI 81%/74th pct. Only 5 onClicks (redact/select/confirm/edit/dismiss); **Submit Review & Clear have none**; Confirm/Edit/Dismiss only `setAction()` (no POST). Backend `review.py` has 9 endpoints, none called for writes. | UI built against mocks; only training-stats were wired, finding body + submit never were. | `web/src/pages/sme/ReviewQueue.tsx:25,98,144,205,218-221,370-378`; `app/routers/review.py:37-215` | N | Y | N | Panel binds selected queue item; Submit POSTs action+note to review endpoint | **TESTED (Phase 2)** |
| SEC-003 | HIGH | VER | 1–2 | `_SERVICE_HEADERS` uses the service-role key everywhere; grep for `set_config`/`SET request.jwt.claims`/`SET ROLE`/`jwt.claims` across `app/` → **no matches**. RLS never fires at runtime. | PostgREST-with-service-role chosen; RLS retrofitted as advisory only; isolation is per-endpoint. | `app/db.py:11-20`; (absence across `app/`) | Y | N | Y | Cross-tenant contract test (shared w/ SEC-001) as the minimum backstop | **PARTIALLY FIXED (Phase 1 min: `customer_org_scope` helper + contract test; full RLS = Phase 2)** |
| SEC-002 | HIGH | REP | 2 | `validate_url` resolves+checks IPs then `return url` (hostname, not IP); fetch does `client.get(current)` → httpx re-resolves at connect. **Not IP-pinned.** | Validation and connection decoupled; checked IP not pinned to the socket. | `app/services/intake/ssrf.py:42-65`; `app/services/intake/extract.py:104-128` | Y | N | N | `validate_url` unit test + IP-pinned-fetch test (reject peer IP ≠ validated) | **TESTED (Phase 2)** |
| BACK-001 | HIGH | REP | 2 | `is_running()` matches any `status=running` row with **no age/started_at cutoff**; `_close_run` only in `try` (no finally, can't survive SIGKILL); lifespan starts/stops scheduler only — **no reaper**. | Liveness inferred from a status column; no heartbeat/lease. | `app/services/jobs/framework.py:78-81,93-97,115,120-137`; `app/main.py:19-24` | Y | N | N | Reaper/age-cutoff test: an old `running` row is treated dead and job re-runs | **TESTED (Phase 2)** |
| EVAL-001 | HIGH | VER | 2 | Harness **exists** (`eval.py` /gold-set, export, /gold-label, import, /finding-precision). Gold set `logs/eval/gold_set_v1.csv` (233 rows) has **empty FILL columns**; `/finding-precision` returns "no SME review actions yet"; no accuracy artifact. Never run. | Gold labels require human SMEs; harness built but never executed; measurement needs live DB. | `db/migrations/0036_gold_label.sql`; `app/routers/eval.py:48-155`; `logs/eval/gold_set_v1.csv`; `tests/test_f17_eval.py` | Y | N | Y | **Process**: label 200-clause gold set, run F17, publish accuracy + VCI calibration | **BLOCKED — EXTERNAL (SME labels + live DB/GPU; harness verified present, runbook in PHASE2-DONE)** |
| ARCH-001 | HIGH | VER | 3 | Intake state = mode/url/text/file only; FormData appends only url/text/file (**no org-name field at all** — report's "optional org name" is now stale). `_find_or_create_org` writes `industry:"unknown", size:"unknown", geography:"US"`. `compute_ic(industry)` reads, never infers. Runbook documents manual DB industry pre-step. | Capture UI/endpoint for org profile & scope never built; scoring falls back to constants. | `web/src/pages/customer/Intake.tsx:63-72,105-110`; `app/routers/assessments.py:394-396`; `app/services/profiling/live_profile.py:130-140,369`; `docs/DEMO_RUNBOOK.md:40-47` | Y | Y | Y | Intake→org industry/jurisdiction thread-through; cohort-match test; retire manual DB step | **PARTIALLY FIXED (ARCH-001A thin slice TESTED — industry+jurisdiction captured & consumed; full multi-step intake = ARCH-001B pending)** |
| FIND-001 | HIGH | VER | 3 | Live path: `FindingInput(… enforcement_matches=[] …)` hardcoded empty → every finding's `enforcement_ids` is `[]`. Only `scripts/generate_findings.py` populates offline. Parked as SME decision. | Live pipeline never threads F-004 matches into `FindingInput`. | `app/services/pipeline.py:172`; `scripts/generate_findings.py:135,199-203`; `DECISION-NEEDED.md:143` | Y | N | Y | **Via spec-update workflow** (SME ruling first); then finding carries enforcement_ids on live path | **BLOCKED — SME APPROVAL (governed; flagged in decision-log; code unchanged)** |

## C. Medium

| ID | Sev | RptSt | Ph | Current-code verification | Root cause | Affected files (current path:line) | B | F | D | Test required | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|
| SEC-004 | MED | REP | 2 | Scheme-only check; `parsed.port or 443` passed to getaddrinfo but **no {80,443} allowlist**; metadata blocked by literal `== "169.254.169.254"`; IPv4-mapped-v6/ULA rely only on is_private/is_reserved. `discover.py` builds `base` from `netloc` (ports flow through). | Port + IPv6-form gaps in the SSRF filter. | `app/services/intake/ssrf.py:26-65`; `app/services/intake/discover.py:80-83` | Y | N | N | Reject non-{80,443} ports, mapped-v6, ULA — unit tests (with SEC-002) | **TESTED (Phase 2)** |
| SEC-005 | MED | REP | 3 | Rate-limit primitives found only in `auth.py` (`_rl_fail` dict + Lock, keyed on `request.client.host`, per-replica). `create_assessment`, `create_bulk_job`, `get_report_pdf` have **no** `_rl_check`. PDF has a concurrency semaphore (503), not per-client throttle. | Limiter only on login; heavy endpoints unthrottled; keys on proxy IP. | `app/routers/auth.py:34-71,132-135`; `assessments.py:143`; `bulk.py:62`; `reports.py:228,261-267` | Y | N | N | Throttle tests on assessments/bulk/pdf; forwarded-IP keying | **TESTED (Phase 4)** |
| SEC-006 | MED | REP | 3 | `brand_color` interpolated raw (after minimal HTML-escape) into a CSS `style="…{color}…"`; `logo_url` into `src=`. `_esc` only replaces `& < > "` — no hex/rgb validation, no https allowlist, no `}`/`javascript:`/`data:` handling. WeasyPrint constrained; Playwright `set_content` path is live Chromium. | Context-unaware escaping of partner branding. | `app/services/report/renderer.py:34-44,233-239,105-127` | Y | N | N | brand_color hex/rgb validation + logo_url https-allowlist + CSS-breakout/data:-URI test | **TESTED (Phase 4)** |
| SEC-007 | MED | REP | 1 | `.env` present, gitignored, never committed. Live values present for SERVICE_ROLE_KEY, JWT_SECRET, DATABASE_URL (embedded pw), TAILSCALE_AUTHKEY. **Divergence:** HOSTED_QWEN_API_KEY is empty/placeholder. | Plaintext prod secrets on dev machine + weak DB pw. | `.env` (root); `.gitignore:2-3` | Y | N | Y | **Process/EXTERNAL**: rotate DB pw + service-role key; move to vault; scope Tailscale key | **BLOCKED — EXTERNAL (rotation); code is env-driven, hygiene verified** |
| CRED-001 | MED | VER | 1 | `git show --stat a54c598` → `local_users.json | 26 +++`; the file held admin@/sme@/customer@visentix.com PBKDF2 hashes+salts, all `organization_id: null`. **Untracked + gitignored since `015ff8e`**, absent from working tree — **but hashes remain permanently in git history** (no rewrite). | Credential material committed before being gitignored. | history `a54c598` (removed `015ff8e`); `.gitignore:39` | Y | N | N | **Process/EXTERNAL**: rotate all 3 creds; confirm no null-org customer in prod; consider history rewrite | **PARTIALLY FIXED · rotation BLOCKED — EXTERNAL** |
| SEC-008 | MED | REP | 3 | Migration 0011 defines `privacy_notice/notice_section/disclosure_clause` SELECT policies (`:4-49`, report's `:23-76` is off). Latest dump has 6 CREATE POLICY but **none of these three**; `rls_state_pre0042` shows them `rowsecurity=False` w/ anon+authenticated SELECT. Ledger backfills 0011 as applied. | Ledger/reality drift; 0011 policy block not actually present live. | `db/migrations/0011_live_assessment_isolation.sql:4-49`; `db/schema_dumps/live_schema_20260728T202044.sql`; `scripts/db/apply_and_record.py:50` | N | N | Y | Re-run 0011 idempotent block; CI check diffing live `pg_policies` vs migration set | **IMPLEMENTED (Phase 4 — corrective migration 0046 + drift-guard test; prod apply + live pg_policies verify BLOCKED-EXTERNAL)** |
| DATA-001 | MED | REP | 3 | `# (timestamp-based)` … `pop_version = int(time.time())` returned as `benchmark_population_version`; flows to `report_snapshot`. | Wall-clock injected into scoring lineage. | `app/services/benchmark/population.py:244-246,254` | Y | N | N | Two identical-corpus runs yield the same version stamp | **TESTED (Phase 3)** |
| DATA-002 | MED | REP | 3 | `cohort_date = str(date.today())` and `snapshot_id` embedded in Section-1 content; `_content_hash` hashes the whole dict (`json.dumps(..., default=str)`). Hash changes across days/snapshots for identical scores. | Volatile fields inside the hashed content; hash not a canonical subset. | `app/services/report/assembly.py:55-56,68-76`; `app/routers/reports.py:118-121,316-334` | Y | N | N | content_hash stable across days/snapshots for identical scores | **TESTED (Phase 3)** |
| DATA-003 | MED | REP | 3 | `RegulatorExposure.tsx:42` passes `vci={75}` unconditionally; `FindingsTable.tsx:109` `… || 75` fallback when confidence absent. | Trust field defaults to a plausible success value (same anti-pattern as GRD-002). | `web/src/report/sections/RegulatorExposure.tsx:42`; `web/src/report/sections/FindingsTable.tsx:109` | N | Y | N | Render honest absence when VCI/confidence missing (no 75) | **TESTED (Phase 3)** |
| DATA-004 | MED | REP | 3 | No `FOREIGN KEY (organization_id)` on `risk_finding`/`report_snapshot`/`organization_intelligence_profile`; org-FK list covers only privacy_notice/derived_data_item/bulk_job/etc. `risk_finding.organization_id` & `.notice_id` both **nullable**. | Org FKs never added; nullable owners. | `db/schema_dumps/live_schema_20260728T202044.sql` (risk_finding 4193-4213, report_snapshot 4151-4168, org_intel_profile 3939-3960; FK section 6522-6842) | N | N | Y | Additive FK + NOT-NULL (scored rows) migration after row validation | **IMPLEMENTED (Phase 3 — migration 0045 NOT VALID FKs + orphan-audit; prod apply + VALIDATE/NOT NULL = BLOCKED-EXTERNAL)** |
| AI-002 | MED | REP | 3 | Retry tuple = `(ReadTimeout, ConnectTimeout, RemoteProtocolError)`; `raise_for_status()` raises `HTTPStatusError` (not retried) → caught by classify's `except Exception` → `{"category":"other","confidence":0.5}`. | 5xx not in retry set; swallowed to keyword-level. | `app/services/llm.py:135,165,203,77-81` | Y | N | N | Retry on 429/5xx; surface `llm_classified==0` as low-confidence | **TESTED (Phase 3)** |
| AI-004 | MED | VER | 3 | Prompt sends `Categories: {json.dumps(categories)}` = 9 bare slugs (`TAXONOMY_V2`), no definitions/examples; `config/clause_taxonomy.json` (~30 types w/ definitions) never injected. Self-reported confidence (cap 0.95) feeds VCI `nlp` at 30%. | Rich taxonomy not sent to the model; uncalibrated confidence weighted 30%. | `app/services/llm.py:60-102`; `app/services/intake/classify_v2.py:14-17,32`; `config/clause_taxonomy.json`; `app/services/scoring/vci.py:13`; `pipeline.py:74,133-134` | Y | N | N | Inject per-domain definitions/few-shot; (accuracy measured via EVAL-001) | **TESTED (Phase 3)** |
| BACK-002 | MED | REP | 3 | `emit_event` + `deliver_for_event` (loop) run **before** `supabase_rest_patch(content_hash)`; no transaction, no `(notice_id,new_hash)` dedupe. | Alert delivered before persisting the new hash. | `app/services/jobs/monitor_notices.py:93-106` | Y | N | N | No duplicate alert on simulated crash after delivery; hash-first/dedupe | **TESTED (Phase 3)** |
| FE-001 | MED | REP | 3 | `ReportPage.tsx:97-104` uses bare `<a href=…/pdf target=_blank>` (no header). Endpoint `require_role("customer","sme","admin")`. `QuarterlyReport`/`PartnerPortal` already use `getBlob`+objectURL. | Bare navigation can't carry the JWT. | `web/src/pages/ReportPage.tsx:97-104`; `app/routers/reports.py:228-231`; `web/src/pages/quarterly/QuarterlyReport.tsx:196-206`; `web/src/pages/partner/PartnerPortal.tsx:180-186` | N | Y | N | Download uses `api.getBlob` + object URL (authenticated) | **TESTED (Phase 3)** |
| QA-011 | MED | VER | 2 | Single blocking `await api.postForm("/assessments/", …)`; `postForm` is plain `fetch`, no AbortController/timeout. Spinner + error text but no retry/timeout/elapsed/stage progress. | Synchronous intake; no async job/poll. | `web/src/pages/customer/Intake.tsx:95-128,260-270`; `web/src/lib/api.ts:102-112` | N | Y | N | Async 202 + `assessment_id` + status polling test | **IMPLEMENTED (Phase 2; tests pass; prod migration 0043 apply BLOCKED-EXTERNAL)** |

## D. Low / Technical debt

| ID | Sev | RptSt | Ph | Current-code verification | Root cause | Affected files (current path:line) | B | F | D | Test required | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|
| SEC-009 | LOW | REP | 4 | `_hash_key = sha256(plaintext).hexdigest()` (no salt/pepper/HMAC); verify via `key_hash=eq.{hash}` DB string equality. Keys are high-entropy `vsx_`+token_urlsafe(32). | Unsalted SHA-256 bearer-key hashing. | `app/services/partner.py:43-44,100` | Y | N | Y | HMAC-with-pepper; feed-endpoint rate-limit | **TESTED (Phase 4; key rotation to HMAC = external re-issue)** |
| SEC-010 | LOW | REP | 4 | Untyped `dict` bodies at `partner.create_workspace:47`, `partner.create_api_key:188`, `quarterly.build:66`, `eval.post_gold_label:93`. **Divergence:** none found in `admin.py`/`bulk.py` (report implied those). | Untyped bodies bypass Pydantic + size caps. | `app/routers/partner.py:47,188`; `quarterly.py:66`; `eval.py:93` | Y | N | N | Typed models + ingress body-size cap | **TESTED (Phase 4)** |
| SEC-011 | LOW | REP | 4 | `put_notifications` stores `webhook_url` with no `validate_url` (`:53-69`); send path `deliver_for_event`→`_default_http_post` does raw `httpx.post` (no check). **Divergence:** notifications never handles `logo_url` (report inaccurate); logo lives in partner.py (not persisted v1). | Webhook URL a second-order SSRF sink, unvalidated at save+send. | `app/routers/notifications.py:53-69`; `app/services/alerts.py:127-130,176-183`; `ssrf.py:26` | Y | N | Y | validate_url (pinned-IP) at save and at send; scheme/port restriction | **TESTED (Phase 4)** |
| BACK-003 | LOW | REP | 4 | Bare `except:` at `reports.py:483`, `explain.py:182`, `explain.py:321`; silent `except Exception: pass` at `live_scoring.py:219-220` (comment "Table may not exist yet") and `reports.py:142-143`. | Over-broad/silent exception handling. | `app/routers/reports.py:483,142-143`; `app/routers/explain.py:182,321`; `app/services/live_scoring.py:219-220` | Y | N | N | Narrow exception types; log every swallow | **TESTED (Phase 4)** |
| AI-003 | LOW | REP | 4 | `# … proxy — actual element detection is Phase 5` (`:217`); `present_count = sum(1 for e … if e["domain"] in ctx.domains_present)`; `base = present_count/total_expected*100`. Presence-proxy, not quality. | Analysis-depth limitation (feeds ARCH-001), not a bug. | `app/services/scoring/formulas.py:217-224` | Y | N | N | Element-level presence + clause-quality signals (Phase 5) | **DOCUMENTED (Phase 4 — governed; Phase-5 spec item)** |
| F14-001 | LOW | VER | 4 | `total_findings_count = sum(1 for s in [f002,f003,f005,f006,f007] if s.score>0)`; `f014 = compute_f014(total, max(total,1), …)` → numerator==denominator → ratio 1.0 on fresh path. | Validation ratio satisfied before SME review. | `app/services/pipeline.py:145-147` | Y | N | N | Recompute F-014 post-SME-clear or label pre-review | **TESTED (Phase 4 — labeled pre_review)** |
| DB-001 | LOW | VER | 4 | `ls` shows three `0011_*`, two each `0012_/0013_`, `0022→0024` gap. Order = hand-maintained `HISTORICAL_APPLIED`+`APPLY_NOW` lists, not filename sort (e.g. 0017 before 0014). | Non-monotonic numbering + manual ordering. | `db/migrations/*`; `scripts/db/apply_and_record.py:39-88` | Y | N | Y | Manifest-derived apply order; monotonic renumber | **DOCUMENTED (Phase 4 — numbering rule + aliases; no rename on deployed DB)** |
| DB-002 | LOW | REP | 4 | `assessment_id text` in `assessment_review:3362`, `review_queue_item:4177`, `training_label:4339`, `report_snapshot:4166`, and `approve_and_freeze(p_assessment_id text)` RPC `:789`. Only assessment_id FK is `bulk_job_row` (uuid). None of the five FK-checked. | `text` vs `uuid`+FK inconsistency. | `db/schema_dumps/live_schema_20260728T202044.sql` (3362,4166,4177,4339,789) | Y | N | Y | Standardize on uuid → privacy_notice(notice_id); additive | **IMPLEMENTED (Phase 4 — CHECK-uuid NOT VALID 0047; text→uuid conversion staged BLOCKED-EXTERNAL)** |
| MAINT-001 | LOW | VER | 4 | `NewAssessment.tsx` unrouted (no import/route in App); `web/.env` present w/ dead `VITE_SUPABASE_URL`/`ANON_KEY`; `onedrive.py` no importers. | Dead code / stale env. | `web/src/pages/NewAssessment.tsx`; `web/.env:1-2`; `app/services/onedrive.py` | Y | Y | N | Delete all three; build/tsc/vitest stay green | **RESOLVED (Phase 4 — deleted; gate green)** |
| QA-012 | LOW | VER | 3 | No processing/retention/confidentiality copy anywhere in Intake; only a post-hoc "Verified source" badge. | Disclosure copy never added. | `web/src/pages/customer/Intake.tsx:130-417` | N | Y | N | Short processing/retention/confidentiality note on intake | **RESOLVED (Phase 3)** |
| QA-013 | LOW | VER | 3 | No `report_mode`/`report_type`/audience/template selector in intake or config. Dual-register machinery exists only as a read-side **stub** in `ExecutiveSummary.tsx` ("stub until backend supplies"). Only partner-branded variant. | Audience/template selection never built at config layer. | `web/src/pages/customer/Intake.tsx`; `web/src/report/sections/ExecutiveSummary.tsx:19-49` | N | Y | N | Audience/template selection at config layer (pairs w/ ARCH-001) | **DEFERRED (documented — no decorative button; needs backend register narrative + spec)** |

---

## E. Divergences from Report v1.0 (honesty log)

These do **not** overturn any finding; they refine it against current code:

1. **SEC-007** — `HOSTED_QWEN_API_KEY` is empty/placeholder in `.env`, not a live key as the report's "provider API keys" implied. All other named secrets are live.
2. **CRED-001** — status downgraded to **PARTIALLY FIXED**: `local_users.json` was untracked + gitignored in `015ff8e` (post-report), so it is no longer in the working tree; **but** the hashes remain in git history (`a54c598`), so the rotation action still stands.
3. **SEC-008** — policy block is at `0011…:4-49`, not the report's `:23-76`; drift itself is confirmed via the committed schema dump + `rls_state_pre0042` file.
4. **SEC-010** — 4 untyped-`dict` endpoints confirmed (partner ×2, quarterly.build, eval.post_gold_label); **not** found in `admin.py`/`bulk.py` as the report implied.
5. **SEC-011** — the `logo_url` claim is **inaccurate**: `notifications.py` only handles `webhook_url`. The `webhook_url` second-order SSRF (save + send unvalidated) is fully confirmed.
6. **ARCH-001** — the report's "(+ optional org name)" at intake is now **stale**: there is no org-name field at all in `Intake.tsx`.
7. **EVAL-001** — the F17 harness, gold-set migration, and endpoints all exist; the gold set is unlabelled (FILL columns) and no accuracy artifact exists. The "run it / measure it" half is **BLOCKED** on SME labels + a stable live DB/GPU.
8. **DATA-004 / DB-002 / SEC-008** — verified against the most recent committed schema dump (`live_schema_20260728T202044.sql`); live-DB re-confirmation is **BLOCKED** (live Supabase returned PostgREST 500 statement-timeouts during the gate — see `00-BASELINE.md` §4/§6).

---

## F. Cross-cutting couplings (do not split when fixing)
- **SEC-001 + SEC-003** — the leak and the missing-backstop are one fix set; ship the org filter **with** the cross-tenant contract test.
- **GRD-001 + GRD-002 + GRD-003** — wiring the guardrail in (GRD-001) over a filter that still bypasses (GRD-003) while the receipt still lies (GRD-002) ships a known hole; land together. GRD-002+GRD-003 are Phase 1; GRD-001 Phase 2 per the report.
- **SEC-002 + SEC-004** — same file, one SSRF change set (+ the missing `validate_url` unit test).
- **SEC-007 + CRED-001** — one credential-rotation action (EXTERNAL).
- **ARCH-001 + QA-012/QA-013** — the config-capture intake carries the disclosure copy and audience/template modes.

---

## G. Global scan appendix (Step 3)

Repo-wide token scan of `app/`, `web/src/`, `config/`, `scripts/` (excluding node_modules/.venv/dist/caches). Classified A–G; **only categories E (dangerous prod placeholder) and G (unresolved impl) feed new rows.**

### Token counts (prod vs test/fixture)
| Token | Prod | Test/fixture | Dominant class |
|---|---|---|---|
| `TODO` | 4 | 0 | ftc.py×2, compute_f004.py, run_ftc.py |
| `FIXME` / `HACK` / `NotImplemented` | 0 | 0 | — |
| `placeholder` (i) | ~35 | ~9 | mostly UI `placeholder=` attrs + honest-absence copy (A) |
| `mock` (i) | ~18 | ~26 | prod hits are "mock removed/replaced" comments; live `mockData.ts` is gated (D) |
| `hardcode` (i) | ~16 | 1 | all are comments asserting *nothing* hardcoded (A) |
| `fake` (i) | ~20 | 1 | test-injection + Hard-Rule-7 "never fake" comments (A/C) |
| `dummy` | 1 | 0 | seed-script default (B) |
| `127.0.0.1` | 2 | 0 | CORS default (A) + NewAssessment.tsx dev fallback (B) |
| `localhost` | 13 | 0 | CORS/ollama defaults, DEV-guarded API fallbacks (A/B) |
| `"passed"` | 7 | 2 | quarterly gate (A) + explain.py guardrail default (GRD-002) |
| `"unknown"` | 30 | 0 | org industry/size fallbacks (A/B) — ARCH-001 |
| bare `except:` | 3 | 0 | reports.py, explain.py×2 (BACK-003) |
| `console.log` | **0** | 0 | confirmed absent (matches report's praise) |

### Category E / G hits
| Ref | Cat | What | Feeds |
|---|---|---|---|
| `app/services/report/explain.py:497` (& `:515`) | **E** | `"guardrail": exec_meta.get("guardrail","passed")` — fail-open default on a security-relevant, customer-shown field (ExplainPanel renders green "passed"). `NarrativeOutput` never emits a `guardrail` key so it *always* resolves to "passed". | **Existing → GRD-002** (not a new row). Note: `build_explanation_bundle` currently has no `app/` caller (test/latent) — confirm live-reachability when fixing. |
| `scripts/compute_f004.py:158` | **G** | `TODO: wire into VCI enforcement component (15%)` — F-004 enforcement-correlation confidence is computed & stored but **not wired into VCI's 15% enforcement component**. Real missing scoring work. | **NEW → GS-001** |
| `app/services/scoring/formulas.py:52` (& `:124`) | **G (borderline)** | `confidence_score: float = 0.5  # placeholder until VCI (P4.3)` — fixed 0.5/0.3 placeholder confidence, expected to be overwritten by the VCI backfill (`scripts/compute_advanced_scores.py:205`). If it ever surfaces un-backfilled, it's a fabricated confidence number. | **NEW → GS-002** |

### New rows discovered by the scan (not in Report v1.0's 36)
| ID | Sev (proposed) | Verification | Root cause | Files | B/F/D | Test required | Status |
|---|---|---|---|---|---|---|---|
| GS-001 | Low | `# TODO: wire into VCI enforcement component (15%).` — F-004 confidence stored but not consumed by VCI's 15% enforcement weight. Adjacent to FIND-001 (enforcement lineage) and AI/EVAL scoring depth. | Enforcement-correlation confidence never threaded into VCI. | `scripts/compute_f004.py:158`; VCI weights `app/services/scoring/vci.py` | Y N Y | VCI includes the enforcement component when F-004 confidence present | **DEFERRED (Low; scan-surfaced, outside the 36; tied to FIND-001 which is SME-governed)** |
| GS-002 | Low | Placeholder `confidence_score=0.5/0.3` "until VCI (P4.3)"; relies on a later backfill to overwrite. | Placeholder confidence written pre-VCI; correctness depends on backfill fully overwriting. | `app/services/scoring/formulas.py:52,124`; backfill `scripts/compute_advanced_scores.py:205` | Y N Y | Assert no P4.1/P4.3 placeholder confidence reaches a customer-facing payload un-backfilled | **DEFERRED (Low; scan-surfaced, outside the 36; verify backfill coverage before customer exposure)** |

### Scan cross-references (avoid double-counting)
- **DATA-003 (VCI=75):** the token scan did **not** surface it because `vci={75}` / `|| 75` contain no scan token (numeric literal). It is nonetheless **CONFIRMED** by direct file read (`RegulatorExposure.tsx:42`, `FindingsTable.tsx:109`) — the scan's "not found" is a token-coverage artifact, not a contradiction.
- **MAINT-001 (127.0.0.1):** `NewAssessment.tsx:28` is `import.meta.env.DEV ? "http://127.0.0.1:8000" : ""` — DEV-guarded (category **B**), but the file is unrouted dead code, so MAINT-001 (delete) still applies.
- **ARCH-001 ("unknown"):** ~30 sites; ingestion writers set `industry:"unknown"` with explicit "NOT NULL; draft SIC map NOT applied (expert-owned)" comments, and downstream code filters `ind in ("","unknown")` — a *guarded* fallback (A/B), consistent with ARCH-001.
- **BACK-003 (bare except):** the 3 bare `except:` are narrow JSON-parse guards (`except: snap_payload = {}`) — style/robustness, matching BACK-003.
- **ftc.py/run_ftc.py TODOs:** documented expert-owned scaffolds (obligations intentionally deferred pending SME review) — a deliberate design boundary, **not** category G.

