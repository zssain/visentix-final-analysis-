# MOCK TRACKER — the MVP mock-closure punch list

**Version:** 2.1 · 2026-07-27
**Authority:** this is the live, canonical tracker of every mock in the product. It replaces the `MOCK TRACKER` section of the archived `docs/old-docs/UI_SPEC.md`. `00-plan/mvp-completion-plan.md` Workstream A drives these to closure; each feature spec's Mocks section points here.

**Rule (unchanged):** every mock must be replaced with real data before shipping to a real client. Never display a hardcoded score, cohort `n`, snapshot ID, or count. Status values: **Open** (still mocked) · **In progress** · **Replaced** (real data wired, verified).

**Definition of done:** every row below `Replaced`, and `grep` for hardcoded `S-2041`, `n=30`, `142 / 31 / 12` returns nothing (Workstream A exit gate).

| ID | Feature | Screen | What's mocked | Real source | Status | Removal plan |
|---|---|---|---|---|---|---|
| M-01 | F01 | Intake & Decomposition Explorer | ~~Clause extraction simulated with a static JSON fixture~~ | `POST /api/assessments` → real `disclosure_clause` rows | **Replaced** | Verified 2026-07-16 audit: Intake posts to `/assessments/` and renders real decomposition output (sections/clauses/LLM counts); no fixture remains in `Intake.tsx` |
| M-02 | F01 | Intake & Decomposition Explorer | ~~"Verified source" badge not present~~ | Real `ssrf_protected` flag in the intake response | **Replaced** | 2026-07-27: `create_assessment` now returns `ssrf_protected` (true only when the source was a URL that passed SSRF validation — a failed check raises before this point); `Intake.tsx` renders a register-safe "✓ Verified source" badge from it (no security jargon, Rule 9). File/text intake → no badge. |
| M-03 | F05/F06 | Clause Comparison (BenchmarkLanguage) | ~~Exemplar clause hardcoded~~ | `disclosure_clause WHERE is_exemplar = true` | **Replaced** | 2026-07-27: report assembly (`reports.py:_load_exemplar_clauses`) sources exemplars from `disclosure_clause` WHERE `is_exemplar=true AND exemplar_status=approved` (the 16), mapped `domain_id`→legacy_slug via `config/clause_taxonomy.json`, one per domain; a domain with none renders honest absence. ⚠️ **SME re-review still required**: several of the 16 approved exemplars are non-English or appear domain-mismatched (data-quality, not wiring). |
| M-04 | F06 | SME Workbench — de-id checker | ~~Training-label counts hardcoded 142 / 31 / 12~~ | `GET /admin/training-stats` | **Replaced** | Verified 2026-07-16 audit: ReviewQueue + Admin Console read the real `/admin/training-stats` route (exists in `app/routers/admin.py`); no hardcoded counts remain |
| M-05 | F05 | Mobile Advisor view | ~~Advisor Note prose hardcoded house-voice text~~ | Frozen `report_snapshot` Advisor layer | **Replaced** | 2026-07-27: `AdvisorNote` renders `advisor_lede`/`advisor_body` from the frozen snapshot only; hardcoded house-voice fallback removed → honest absence ("not part of this snapshot") when omitted. Regression test asserts prose is never fabricated at render (`report_sections_ws4.test.tsx`). |
| M-06 | F07 | Continuous Monitoring dashboard | ~~Surface does not exist~~ | F-012 trend-delta outputs (`formula_version` + stored `derived_data_item`) | **Replaced** | 2026-07-27: `GET /api/monitoring/trend?org_id` built (`app/services/monitoring.py`) — series from stored `derived_data_item`, deltas via the versioned `compute_f012`; single-snapshot org → `baseline_established` (no fabricated trend). `MonitoringHero.tsx` renders the sparkline + improvement-colored delta on the Dashboard. |
| M-07 | F07 | Continuous Monitoring dashboard | ~~Surface does not exist~~ | `monitoring_event`, org-scoped | **Replaced** | 2026-07-27: `GET /api/monitoring/events?org_id` built; change feed in `MonitoringHero.tsx`. ⚠️ **Live drift** (see schema.md §5.4): `monitoring_event` has no `organization_id` column live, so events are org-scoped via `source_record.url` host ↔ `organization.domain`; `trigger_type` normalized (`hash_change`→`notice_changed`). Score moves lead with from→to numbers. |
| M-08 | F07 | Continuous Monitoring dashboard | ~~Surface does not exist~~ | F-013 outputs + resolved `enforcement_record` | **Replaced** | 2026-07-27: `GET /api/monitoring/alerts?org_id` built — stored F-013 `alert_escalation` joined to **resolved** enforcement only (623 unresolved never surface); each alert carries finding lineage + `formula_version` + VCI. Severity is surfaced only from a stored `monitoring_event.severity` (no F-013→band thresholds invented; see schema.md §5.4). Full AdvisorNote expansion from an alert (finding join) deferred. |
| M-09 | F05/F07 | Report — Cover, Traceability | ~~Provenance ribbon hardcoded `S-2041` / `2026-06-19`~~ | Real `report_snapshot.id` + `snapshot_frozen_at` | **Replaced** | 2026-07-27: `_load_stored_report` injects authoritative `_snapshot_id`/`_generated_at` from the `report_snapshot` row; `ReportView` threads them into the provenance ribbon (preferred over any assembly-time value) and into every section; drafts keep the watermark state. No hardcoded snapshot IDs in `src/` (exit-gate grep clean). |
| M-10 | F05 | Lineage drawer | ~~Formula plain-language descriptions hardcoded~~ | `formula_version.description` column | **Replaced** | 2026-07-27: `GET /api/formulas` serves `formula_version.description` (14/14, 0 NULL); `ReportView` fetches once and threads a `formula_descs` map into every section, so `RiskDashboard`/`CompoundRisk`/`FindingsTable` lineage drawers read real descriptions (hardcoded `FORMULA_DESCS`/inline copy removed); backend down → honest empty. |
| M-11 | F08 | Finding Codex | ~~Codex entries are a static JSON array~~ | `GET /findings/codex` over the `finding_type` catalog | **Replaced** | Verified 2026-07-16 audit: FindingCodex page and CodexTooltip both read the real `/findings/codex` route (exists in `app/routers/findings.py`) |
| M-12 | all | All screens | ~~Cohort size shown as `n=30`~~ | Live `benchmark_membership` count | **Replaced** | 2026-07-27: cohort `n` flows from the payload's live count (retail 25 / healthcare 31 / fintech 23); the two vitest fixtures that still carried `n=30`/banned dates were changed to clean synthetic values (25/23, 2026-07-18/20). Exit-gate grep for `S-2041` / `n=30` / `142/31/12` is clean across `src/`. |
| M-13 | F09 | Admin Console | ~~Global Gate Mode simulated locally in React~~ | `GET/POST /review/gate-mode` + `platform_setting` | **Replaced** | 2026-07-27: `Console.tsx` GETs `/review/gate-mode` on load and POSTs on change (optimistic switch with rollback + honest error on failure). **Actual path is `/review/gate-mode`** (not `/api/admin/gate-mode` — see F09 A1 note); `platform_setting` empty → defaults to `instant_draft` until first write. |
| M-14 | F09 | Admin Console | ~~`trigger-assessment` was a `not_implemented` stub~~ | `POST /admin/trigger-assessment` | **Replaced** | 2026-07-27: real batch (`app/services/reassessment.py`) — reconstructs each notice's `DecomposedNotice` from stored `notice_section`+`disclosure_clause` and runs `live_scoring.score_and_persist`; records an `ingestion_run` row as the run identifier; admin-gated; returns `{run_id, scored, failed, outcome, notices[]}`. Console "System Operations" button wired to it (org_id input → run summary). |
| M-15 | F12→F21 | Quarterly Report reader page | ~~Publication snapshot id + cover corpus counts (orgs / industries / jurisdictions / clauses) hardcoded in `mockData.ts`~~ | Frozen `quarterly_snapshot` + `quarterly_metric` (S4-001..004), DIR-010 | **Replaced** | 2026-07-28 (F21): real `quarterly_snapshot`/`quarterly_metric` (migration 0040), corpus counts are the stored S4-001..004 metric values; snapshot frozen + immutable (service guard + DB trigger); `mockData.ts` deleted. |
| M-16 | F12→F21 | Quarterly Report reader page | ~~Five named Intelligence Indicators + QoQ deltas hardcoded~~ | Market-average aggregates per `formula_version` | **Replaced (v1 subset)** | 2026-07-28 (F21): **DMI (S4-005) + AI Transparency (S4-006)** computed as confidence/industry-weighted market averages; **QoQ deltas + ESI/CRI/CRCS deferred** (baseline = no prior snapshot; consumer-rights/exposure indices need a live formula / expert sign-off — see F21 deferred list). |
| M-17 | F12→F21 | Quarterly Report reader page | Section aggregates — industry rankings, regulator activity, AI-governance trend, disclosure trends, compound patterns | Corpus aggregation from `derived_data_item` / F-012 deltas | **Replaced (v1 subset)** | 2026-07-28 (F21): **Top disclosure gaps** (finding frequencies) + **enforcement theme shares (S4-018, resolved-only)** live. Industry rankings / regulator grid / AI 6-quarter trend / compound patterns **deferred** with named inputs (F21). |
| M-18 | F12 | Quarterly Report reader page | Benchmark Spotlight excerpts hardcoded | SME-approved + de-identified exemplars above the minimum-sample threshold (F06 pipeline) | Open (deferred by F21) | Needs enough SME-approved de-identified exemplars per domain ≥ n=10 (currently 9 approved, several domain-mismatched) — see F21 deferred list |
| M-19 | F11→F20 | Partner Portal | ~~Partner contract + client workspaces (usage, status, branding flags) hardcoded in `mockData.ts`~~ | `partner` + `partner_workspace` (migration 0039), scope-isolated by `partner_id` claim (DIR-005) | **Replaced** | 2026-07-28 (F20): real tenancy — `partner_admin` role + `partner_id` claim (mirrors `organization_id` in `auth.py`); `POST/GET /partner/workspaces` resolve through the one-workspace-wraps-one-client bridge; cross-partner invisible (tested). `mockData.ts` deleted. |
| M-20 | F11→F20 | Partner Portal | ~~API keys + per-contract usage / rate limits~~ | `partner_api_key` (hash only) + `feed_access_log` | **Replaced** | 2026-07-28 (F20): `POST/GET/DELETE /partner/api-keys` — plaintext returned once, only sha256 hash stored, immediate revocation; feed auth hash-compares the `X-Partner-Key` header; every served feed call logged. (Per-contract rate limits still future work.) |
| M-21 | F11→F20 | Partner Portal | ~~Anonymized feed catalog (schema, refresh, permitted-use, VCI, suppression flag)~~ | Per-cohort aggregates from `derived_data_item`, min-sample suppressed (DIR-006 / OD-05 n=10) | **Replaced** | 2026-07-28 (F20): `/feed/white-label` reshaped to `(industry × object_type)` aggregates — `organization_id` removed, `population_n` added, `n<10` suppressed server-side, same population exclusions as the benchmark/quarterly cohort (CQS-eligible, non-rehearsal, live), `schema_version` bumped to `vicbnf-3.0.0`. |
| M-22 | F11→F20 | Partner Portal | ~~Branding config + report templates~~ | `partner` branding + `report_snapshot.branding_applied` applied by the renderer | **Replaced** | 2026-07-28 (F20): `PUT /partner/branding` (logo 2 MB image-validated + color); `GET /partner/reports/{snapshot_id}.pdf` renders a header band only — body numbers unchanged; branding frozen at approval → byte-identical per snapshot. |
| M-23 | F12→F19 | Bulk Analysis | ~~Batch results — ranked company queue (exposure score, VCI, cohort n, top issues) hardcoded in `mockData.ts`~~ | Real batch pipeline (`services/bulk.py`) over the verified reassessment kernel; scores from stored `derived_data_item`/`report_snapshot`/`risk_finding` | **Replaced** | 2026-07-28 (F19): `pages/bulk/BulkAnalysis.tsx` now drives Upload→Job list→Results grid off `POST/GET /bulk/jobs*`; `mockData.ts` deleted. Per-row intake (shared F01 helper) → `trigger_reassessment` → `score_and_persist` (single scoring path). Draft-grade throughout; VCI suppression in JSON + CSV; results tenant-scoped by job ownership. |
| M-24 | F12→F19 | Bulk Analysis | ~~Clause-level evidence snippets per flag~~ | Real stored `risk_finding` (code/domain/severity) surfaced as `top_findings` per succeeded row, with VCI | **Replaced** | 2026-07-28 (F19): results grid `top_findings` come from stored `risk_finding` rows (score-cell click opens the draft report for full clause evidence); export CSV carries the SME not-expert-reviewed notice and zero verdict terms (guardrail extended to the export). |
| M-25 | F13 | Framework Crosswalk | Crosswalk mappings (domain/code → framework citation + note) hardcoded in `mockData.ts` | `framework_reference` table + `finding_type`, via `GET /api/crosswalk` | Open | Build mapping table + endpoint; expert signs off descriptive copy (OD-01), then swap |
| M-26 | F14→F18 | Trust Language Studio | ~~Rewrite prompts (per-domain gap status, current excerpt, suggested pattern, rationale, cohort n) hardcoded in `mockData.ts`~~ | Real F18 clause-rewrite engine over `disclosure_clause` + approved exemplars | **Replaced** | 2026-07-28 (F18): `/rewrite` is now the real guardrailed engine — `POST /assessments/{id}/clauses/{cid}/rewrite` (LLM restructure → banned-term guardrail → fabrication verification → approved-exemplar fallback), token diff + non-dismissible watermark; `clause_rewrite` table (migration 0041). `mockData.ts` deleted. |
| M-27 | F15 | Public Trust Center | Trust-metrics strip values (formulas versioned, reproducibility, lineage fields, review gate) hardcoded in `mockData.ts` | System/publication metadata (`formula_version`, frozen publication snapshot), via `GET /api/trust-metrics` | Open | Wire endpoint to real counts; omit any metric below the suppression threshold (Hard Rule 7) |
| M-28 | F16 | Vendor Due Diligence | Vendor queue + per-vendor procurement summary, evidence-backed signals, and decision state hardcoded in `mockData.ts` | `vendor` + `vendor_review` tables over real assessment output (`risk_finding`, scores), via the vendor endpoints | Open | Build vendor intake→assessment pipeline + review persistence; wire the endpoints |

## Changelog
- 2.1 (2026-07-27): **Engineering closeout — every MVP mock (M-01..M-14) now Replaced.** Built/wired (code-verified, full suite green): M-06/07/08 monitoring backend (`app/routers/monitoring.py` + `MonitoringHero.tsx`) — trend (F-012, versioned `compute_f012`), events, alerts (F-013 + **resolved** enforcement only); M-14 real batch (`app/services/reassessment.py` → `ingestion_run` run_id); M-13 Console↔`/review/gate-mode`; M-03 exemplars from `disclosure_clause.is_exemplar`; M-10 `GET /api/formulas` → real `formula_version.description`; M-05 advisor prose snapshot-only; M-09 authoritative snapshot id/frozen-at threaded; M-02 `ssrf_protected` verified-source badge; M-12 fixtures cleaned. WS-B: BenchmarkLanguage diff toggle + guardrails test over Recommendations/RiskReduction. **Live drift reconciled** in schema.md §5.4 (monitoring_event has no org_id; `alert` table absent). **Still human-gated:** SME re-review of the 16 exemplars (some non-English/mismatched) + crosswalk rows + OD-01..05; OD-09 open; no client snapshot frozen. M-15..M-28 stay Open (post-MVP; mockData confined to their own page dirs).
- 2.0 (2026-07-27): Pilot-readiness data pass. **M-03 → In progress** (16 de-id-passing exemplars approved across 8/8 demo domains; `disclosure_clause.is_exemplar` now exists + populated). **M-10 → In progress** (all 14 `formula_version.description` populated, guardrail-safe). **M-12** data now real: three demo cohorts (retail 25 / healthcare 31 / fintech 23) with live-queryable `benchmark_membership` counts (no static n). All ai_reviewed — frontend wiring + human SME re-review still required before these rows go Replaced.
- 1.9 (2026-07-27): Phase-0 live audit trued up two rows against code. **M-13 → In progress**: backend gate-mode endpoints are now real (`GET/POST /review/gate-mode`, `platform_setting`-backed) — the tracker's "build the endpoints" plan is done; the actual path is `/review/gate-mode` (not `/api/admin/gate-mode`); only the `Console.tsx` frontend wiring remains (still simulates locally). **M-14 stays Open**: `POST /admin/trigger-assessment` confirmed still a `not_implemented` stub; UI placeholder is honest. No status set to Replaced.
- 1.8 (2026-07-16): Audit of the pre-session UI trued up the registry: M-01, M-04, M-11 → **Replaced** (real routes verified in code and backend); M-02 corrected (badge surface absent, not mocked); M-06–M-08 corrected (monitoring-hero surfaces do not exist in the current Dashboard); M-14 corrected (simulation already removed for an honest placeholder).
- 1.7 (2026-07-16): Registered M-28 for the F16 Vendor Due Diligence workflow, built UI-only against mock vendors ahead of the vendor pipeline + persistence.
- 1.6 (2026-07-16): Registered M-27 for the F15 Public Trust Center, built UI-only against mock trust-metrics ahead of the metrics endpoint.
- 1.5 (2026-07-16): Registered M-26 for the F14 Trust Language Studio, built UI-only against mock patterns ahead of the authored pattern library + backend.
- 1.4 (2026-07-16): Registered M-25 for the F13 Framework Crosswalk Explorer, built UI-only against mock citations ahead of the `framework_reference` backend + OD-01 copy sign-off.
- 1.3 (2026-07-16): Registered M-23–M-24 for the F12 Bulk Analysis workflow, built UI-only against mocks ahead of the batch pipeline.
- 1.2 (2026-07-16): Registered M-19–M-22 for the F11 Partner Portal, built UI-only against mocks ahead of the tenancy/metering/feed backend.
- 1.1 (2026-07-16): Registered M-15–M-18 for the F12 Quarterly Report reader page, built UI-only against mocks ahead of the aggregation backend. Every displayed figure on `/quarterly` is mocked; real sources and removal plans recorded per row.
- 1.0 (2026-07-16): Migrated the MOCK TRACKER out of the archived `UI_SPEC.md` into the live spec system, keyed each row to its owning feature spec, and added an explicit Status column so MVP mock-closure is measurable inside the specs.
