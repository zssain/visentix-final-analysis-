# Open Decisions (OD) Register

**Version:** 1.6 · 2026-08-31
**Authority:** the live register of product/content decisions that block MVP completion. Each OD has a recommendation to unblock, an owner, and a status. When an OD is decided, set Status = **Decided** with the date and the outcome, and reflect it in the feature spec(s) it governs. `mvp-completion-plan.md` Workstream C references this file.

**Status values:** **Open** (undecided) · **Recommended** (a recommendation is on the table, awaiting sign-off) · **Decided** (outcome recorded; propagate to specs).

| ID | Decision needed | Recommendation to unblock | Owner | Status | Governs |
|---|---|---|---|---|---|
| OD-01 | Framework Crosswalk copy | Approve **descriptive-only** language ("relates to CCPA §1798.120"); ship the shell now, copy later | Product | **Decided (2026-07-27, ai_reviewed)** — adopt descriptive-only copy | F13 (shell shipped on mock citations), F05 |
| OD-02 | Reader-register names | **Executive / Practitioner / Plain-language**; ship flag-gated behind a feature flag | Product | **Decided (2026-07-27, ai_reviewed)** — Executive/Practitioner/Plain-language, flag-gated | F05 (ExecutiveSummary reader toggle) |
| OD-03 | Advisor-hero-on-mobile default | Approve with the two specced mitigations (thumb-tap switch, full-screen lineage sheet) | Product | **Decided (2026-07-27, ai_reviewed)** — advisor-hero on mobile with both mitigations | F05, design-system §3 |
| OD-04 | Real SME names in attribution | Keep house persona **"The Visentix Privacy Desk"** for MVP; revisit at first paying client | SME team | **Decided (2026-07-27, ai_reviewed)** — keep "The Visentix Privacy Desk" | F06, design-system DDR-003 |
| OD-05 | Low-confidence cohort `n` | Confirm **n = 10** as `LOW_CONFIDENCE_COHORT_N` (conservative vs VICBNF <20 caution band) | Data | **Decided (2026-07-27, ai_reviewed)** — `LOW_CONFIDENCE_COHORT_N = 10` | design-system §2, intelligence-logic §8 |
| OD-09 | Canonical industry for "Entertainment & Media" | Phase-1 config review found `sic_industry_map` SIC ranges 2700-2799 & 7800-7999 ("Entertainment & Media") have **no equivalent** in the canonical 10-industry taxonomy (IND-01..IND-10, `config/org_profile_weights.json`); their draft IND-06 collided with canonical *Insurance*. Left `mapped_by='draft'`, `industry_id='IND-00'` (unmapped). **Recommendation:** add a media/entertainment industry to the canonical taxonomy, or fold these ranges into an existing one. No recommendation adopted — engineer/expert to decide | Expert + Engineer | Open | `config/org_profile_weights.json`, `sic_industry_map`, intelligence-logic §2 |
| OD-06 | `security_event` separation from enforcement | **Proposed:** breach/security-incident reports are org-risk signals, NOT enforcement actions — store in `security_event`, and **never** populate `enforcement_record` or feed **F-004** without a separate, expert-approved formula change | Expert (SME) | Recommended | F02 v2, schema §2.9 |
| OD-07 | Canonical benchmark-cohort table name | Live DB exposes **`benchmark_cluster`**; schema.md names **`benchmark_population`**. Decide the canonical name going forward (rename vs adopt-live). No recommendation — engineer to confirm against live + code | Engineer | Open | schema §2.6, F03 |
| OD-08 | Canonical gate-mode enum | Spec (business-logic §5) says `instant_draft` / `expert_review`; code (`app/services/review.py`) implements `STRICT` / `INSTANT_DRAFT` / `CLIENT_REVIEWS`. Pick one canonical set; do not assume either. No recommendation | Expert + Engineer | Open | business-logic §5, F06, F09 |
| OD-10 | F-005 observed-element semantics | **(PROPOSAL — needs expert calibration):** keep and explicitly label the domain-presence proxy for the pilot; require governed element detection before describing F-005 as element-level quality | Expert + SME | Recommended | intelligence-logic §7, F04, F05 |
| OD-11 | F-002 disclosure-severity semantics | **(PROPOSAL — needs expert calibration):** retain clause share as concentration/context for the pilot; do not call it quality severity until SME-labelled quality signals are governed | Expert + SME | Recommended | intelligence-logic §§7/9, F04, F05 |
| OD-18 | PDF byte-identity is intermittently false (L-013) | Two renders of the same frozen snapshot differ ~6 times in 10; the differing bytes are inside a compressed **font-program stream**, i.e. WeasyPrint's subsetter orders glyphs nondeterministically. Pre-existing and unrelated to recent changes (measured 6/10 on the unmodified tree; not fixed by `PYTHONHASHSEED=0`). **This contradicts a promise the report makes to the reader in plain words** and that F05 AC-1 + Hard Rule 6 assert. Options, none adopted: (a) pin/patch the font subsetter, (b) embed fonts unsubsetted (larger files, likely deterministic), (c) canonicalize the PDF post-render, (d) narrow the customer-facing claim from *byte*-identity to *content*-identity and re-word the Traceability copy. **Until this closes, do not cite byte-identity to a customer or regulator.** No recommendation — engineer + expert | Engineer + Expert | **Open — blocking a reproducibility claim already in print** | F05 AC-1, Hard Rule 6, `app/services/report/renderer.py`, `tests/test_pdf_determinism.py`, Traceability copy |
| OD-19 | Where the clause decomposition explorer lives | Intake became a single-column form and its results pane was removed (owner-decided), which took the **split-pane original-document vs extracted-clauses view** with it — F01's "lineage visible from the first interaction". F01 AC-1/AC-4 now have no surface. Options: fold it into the report's Cover & Scope part · a dedicated `/assessments/:id/clauses` view · or drop it and rely on the report's per-finding evidence stacks. **The capability still exists server-side (clauses are persisted with lineage); only the view is gone.** No recommendation — product to decide | Product + Engineer | Open | F01 AC-1/AC-4, F05, `web/src/pages/customer/Intake.tsx` |
| OD-20 | Whether a categorical chart palette is adopted | The owner-supplied chart tokens `--chart-1…5` are a **sequential** ramp — one hue, lightness 0.845 → 0.432 — correct for magnitude (heatmaps, meters, part-to-whole) and unusable for identity: five shades of one green cannot tell entities apart. No categorical palette exists. Any future surface comparing distinct entities by colour needs one, and **generating hues to fill the gap is expressly forbidden** (a generated hue is indistinguishable from an existing one under CVD). Options: adopt a validated categorical set, or forbid categorical encoding outright and require faceting/small multiples. No recommendation — design call | Owner + Engineer | Open | design-system §1.3, any future multi-series chart |
| OD-12 | Section 4 peer-position measure | **(PROPOSAL — needs expert calibration):** confirm PGMS for F-003/F-011 and label it explicitly; keep F-010 on headline surfaces | Expert + Product | Recommended | intelligence-logic §§5-7, F03, F04, F05 |
| OD-13 | Traffic-light standing tokens (Green / Yellow hexes) | Adopted **Green `#0E7C57` · Yellow `#A87400` · Red `#B42318`**, ink-weight so a score clears AA as text on Soft White (the superseded pastels did not — scores read washed out). Draft-watermark collision resolved: gold stays the watermark and now carries judgement nowhere, so the roles never overlap. Band thresholds unchanged | Owner + Engineer | **Decided (2026-08-31, owner)** — shipped in `scoreBands.ts` + `index.css`; residue tracked as OD-17 | design-system §1/§2, `web/src/lib/scoreBands.ts`, every score surface |
| OD-17 | One standing palette across screen and PDF (+ the band-threshold mismatch) | **Narrowed 2026-08-31.** The *screen* half is settled: one oklch token layer (`web/src/theme.css`), standing values verified against WCAG AA per mode by `scripts/check_contrast.mjs` in CI. Two defects were fixed in the process (`--mid` was shipping at 4.07:1 as text; light standing values measured 2.64–3.34:1 on the dark card). **What remains open is the PDF.** Tailwind cannot reach WeasyPrint and WeasyPrint does not parse `oklch()`, so the PDF keeps its own stylesheet — owner-confirmed 2026-08-31, "closer to what we will be making". Its `_RAMP` still uses its own hexes over **four** bands at 25/50/75 against the web scale's 45/70. Required: generate the PDF's hex values **from `theme.css`** rather than hand-copying, so they cannot drift. The palette half is a design call; **the threshold half is Hard Rule 3** (score bands come only from `intelligence-logic.md`) and is expert-owned. Do not change thresholds unilaterally | Expert + Engineer | **Open — screen half closed, PDF half outstanding** | design-system §1.5, `app/services/report/renderer.py` `_RAMP`, F05 PDF parity, intelligence-logic §3 |
| OD-14 | Customer-facing vocabulary: dimension names + peer-position words | Two linked naming decisions the acronym rule forces. (a) **Plain-English dimension names** — proposed in intelligence-logic §2 (Regulatory attention, Privacy programme maturity, Organizational scale, Data sensitivity, Enforcement history, AI governance maturity); confirm or replace. (b) **Peer-position comparative words** — proposed set *well below / below / about / above / well above average*, mapped from F-011 Benchmark Percentile. **The percentile cut-points for each word are deliberately NOT proposed** (Hard Rule 3 — expert-owned calibration). Nothing renders until both halves close | Expert + Product | Open | intelligence-logic §§2/3, design-system §2/§4, F05 |
| OD-15 | Risk-horizon taxonomy (bills / scheduled / current / enforcement / litigation) | Owner feedback distinguishes a bill under consideration (emerging) from law in force (current), regulator action, and private litigation. Structured as **PROPOSED** in intelligence-logic §13 — context and sequencing only, no formula, weight, threshold, or finding code. Open modelling questions: whether pre-law bills get their own table or ride `obligation` with a status flag (**no live table today**); whether "demonstrated" (enforcement) and "contested" (litigation) are horizons or an orthogonal evidence-strength axis; and whether horizon ever influences finding/recommendation ranking — which would be a scoring change requiring a formula version. No recommendation adopted | Expert + Engineer | Open | intelligence-logic §13, F05 §12, schema (`obligation`, `enforcement_record`, `litigation_event`) |
| OD-16 | Authoritative source of obligation modality (must vs should) | business-logic §2 now requires modality to be **carried from a citable source, never assigned** — but nothing in the schema records whether a given obligation's source text says "shall/must" or "should". Decide where modality lives (an `obligation` column populated at ingest from the source language? a per-requirement_type default? SME-labelled?) and who owns it. **Until this closes, no customer-facing surface may display a "must"** — everything renders as "should" with provenance. No recommendation — expert + engineer to decide | Expert + Engineer | Open | business-logic §2, schema `obligation`, F05 |

## How an OD closes
1. Owner approves the recommendation (in Teams, per our feedback method).
2. Set Status = **Decided (YYYY-MM-DD)** here with the chosen outcome.
3. Update every spec in the **Governs** column, and remove any flag-gating / placeholder the recommendation introduced.
4. Record one line in `logs/decision-log.md`.

## Changelog

- 1.6 (2026-08-31): **OD-17 narrowed** — the screen half is closed (one oklch token layer; AA verified per mode by a CI guard; two live contrast defects fixed), leaving the PDF half outstanding: the print stylesheet stays separate (owner-confirmed) but must be *generated* from `theme.css`. Added **OD-20** (no categorical chart palette exists; the supplied ramp is sequential and generating hues is forbidden). Source: owner decision to rebuild the UI on shadcn.
- 1.5 (2026-08-31): **OD-13 Decided (owner)** — traffic-light standing values adopted and shipped; draft-watermark collision resolved (gold keeps the watermark, carries judgement nowhere). Added **OD-17** for the residue surfaced while shipping it: the PDF renderer's ramp disagrees with the web scale on both hexes and band thresholds, breaking screen↔PDF parity; the threshold half is expert-owned under Hard Rule 3.
- 1.4 (2026-08-31): Added **OD-13** (Green/Yellow standing hexes + the DDR-001 gold-draft-vs-yellow-standing collision), **OD-14** (plain-English dimension names + peer-position comparative words; percentile cut-points expressly left to the expert), **OD-15** (risk-horizon taxonomy — proposed as context only, with the modelling questions left open), and **OD-16** (where obligation modality lives; blocks any customer-facing "must"). All Open; each is referenced by a spec that explicitly refuses to render the affected content until it closes. Source: owner (product) verbal feedback pass.
- 1.3 (2026-08-21): Added OD-10–OD-12 for F-005 observed-element semantics, F-002 disclosure-severity semantics, and the Section 4 peer-position measure. Proposals only; nothing applied. See `DECISION-NEEDED-F005-PRESENCE-PROXY.md`.
- 1.2 (2026-07-27): **OD-01–OD-05 Decided (ai_reviewed)** by the interim AI reviewer during the Phase-1 pilot-readiness pass, adopting each standing recommendation verbatim; propagated to the governed specs (F13, F05, F06, design-system §2/§3/DDR-003, intelligence-logic §5). Attributed `ai_reviewed` — **not** the human owner's Teams sign-off, which is still required to promote these from ai_reviewed to a full content-gate approval (do not treat as expert-approved for client delivery). Added **OD-09** (no canonical industry for "Entertainment & Media", surfaced by the `sic_industry_map` review — Open, expert + engineer). See `logs/decision-log.md` 2026-07-27.
- 1.1 (2026-07-20): Added **OD-06** (`security_event` separated from enforcement / F-004 — proposed, expert), **OD-07** (canonical `benchmark_cluster` vs `benchmark_population` name — engineer), **OD-08** (canonical gate-mode enum, spec vs code — expert + engineer). From the ingestion-architecture amendment (schema v1.3 + F02 v2) and `logs/audits/2026-07-data-layer-audit.md`.
- 1.0 (2026-07-16): Promoted the OD table out of `mvp-completion-plan.md` Workstream C into a standalone register with an explicit Status column and a close-out procedure, so decided/undecided state is tracked rather than static.
