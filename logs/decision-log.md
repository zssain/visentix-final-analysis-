# Decision Log — append-only, newest first

Format: `YYYY-MM-DD · who · decision · one-line why`

- 2026-07-28 · Claude (engineer) + owner (approved: Option 2) · **F05/F18 prereq unblock — bounded demo-cohort embed + obligation match.** Gate was unmet: `clause_obligation` empty (embeddings only 11.5%, matcher ≥95% gate). Embedded the **demo COHORT** clauses (benchmark_membership retail→healthcare→fintech) on **MPS** — same model + version string as the RunPod plan (`all-MiniLM-L6-v2`); **device recorded as provenance** (`device=mps` in `logs/eval/obligation_match_scope.json`). **DISCLOSED before running:** MPS vs CUDA are NOT bit-identical for the same model version (FP non-associativity across backends, ~1e-6/component) but post-normalization cosine is stable to ~4–5 dp → matcher (0.35 floor, top-k) outcomes unaffected; for the quarterly device-consistent full build the MPS rows should be force-re-embedded on CUDA. Embedded 7,250 clauses → cohorts 100% covered. Ran cohort-scoped matcher (tested `match_clauses_to_obligations`, floor 0.35 UNCHANGED) → **57,130 clause_obligation rows** (retail 10,777 / healthcare 10,442 / fintech 35,911), each with matched_terms + model_version. **Scope recorded** so the evidence assembler treats out-of-scope orgs as honest absence ("obligation context not yet available"), never "no obligations matched". Approved exemplars: 9 (verified). **Gate CLEARED → proceeding to F05/F18 (specs first, STOP for approval).** RunPod full-corpus resume path documented (checkpoint 11.5%, `--tranche all` auto-CUDA).

- 2026-07-28 · Claude (engineer) + owner (approved: crawl) · **F07 corpus growth — SaaS crawl executed.** After owner approval, inserted 21 `crawl_target(sector='saas')` rows (22 proposed; 1 pre-existing dedup) and ran `open_web` (Playwright). **Result: 20 captured / 21 crawled, 1 no_notice** (salesforce.com — homepage HTTP 403, honest). All 20 → `organization(industry='saas', origin='open_web')` + fresh `privacy_notice(notice_type='open_web')` + decompose classification; profiled via `compute_profiles_scaled.py --industry saas --fresh-only` → **20/20 have an intelligence profile + CQS-fresh open_web notice = cohort-eligible.** Data-only (no code change). Licensing: companies' own public policies.

- 2026-07-28 · Claude (engineer) · **F07 frontend (commit 3 of 3).** `/admin` gains a **Jobs panel** (`JobsPanel.tsx`): table of jobs (enabled toggle, cron, last-status chip teal/red/gray, counts, Run-now → POST 202, optimistic spinner, polls GET /admin/jobs every 5s while any job runs) + a **banner** when `alert_delivery.status='suppressed_no_threshold'` exists ("Alert delivery is paused pending severity-threshold approval"). `/monitor` change-feed rows gain a **delivery chip** (emailed / webhook / in-app only) from a new org-scoped `GET /api/monitoring/deliveries`, and a **Notifications card** (`NotificationsCard.tsx`: email + webhook + Send-test, honest suppressed/failed toast). Backend hooks: `/admin/status` now returns `alerts_suppressed`; `api.put` added. Mirrors existing pages/customer fetch/error conventions. tsc clean; vitest 86 passed; backend affected 34 passed. Completes F07 (backend PR #8 + corpus PR #9 + this).

- 2026-07-28 · Claude (engineer) · **F07 corpus growth (commit 2 of 3).** **Litigation connector SKELETON** (`app/services/ingestion/connectors/litigation.py`): parses a public CourtListener/RSS feed into the `litigation` table (0037) with `reliability='low'`, `issue_tags=NULL`. **NOT wired to scoring** — using litigation in any score requires an **[EXPERT] weighting scheme first** (no default invented). Parse functions are pure + tested (`tests/test_litigation_connector.py`); the network fetch is guarded on `COURTLISTENER_TOKEN` (stores nothing without it). **Crawl targets GATED:** proposed 22 public SaaS privacy-notice domains in `DECISION-NEEDED-F07-CRAWL.md` — **NOT inserted, NOT crawled** (MUST NOT crawl unapproved domains); the `open_web` crawler runs only after in-session "approved: crawl". Frontend is commit 3.

- 2026-07-28 · Claude (engineer) · **F07 completion — backend (commit 1 of 3).** Migration 0037: `job_run`, `alert_delivery`, `org_notification_setting`, court-filing SKELETON `litigation` (NOT wired to scoring — **expert weighting needed** before use), + **additive** `monitoring_event.organization_id/event_type/payload` (owner-approved: extend rather than a side table; nullable, no backfill — old rows stay URL-scoped) + `privacy_notice.monitoring_enabled`. In-process **APScheduler** (SQLAlchemy Postgres job store; `SCHEDULER_ENABLED` off by default so tests never start it) runs monitor_notices/pull_regulators/refresh_benchmarks under a `job_run` ledger + concurrency guard; cadence/enabled from `platform_setting`. **Reconciliation:** task said emit `cohort_refreshed` but schema's four are notice_changed/score_moved/regulator_signal/**cohort_rebenchmarked** and "add none" → used cohort_rebenchmarked. New deps: APScheduler 3.10.4 + SQLAlchemy 2.0.36. **Alert pipeline:** F-013 escalation; `platform_setting.f013_severity_thresholds` UNSET → `alert_delivery(suppressed_no_threshold)`, **nothing sent** (MUST NOT set thresholds — expert-owned; admin banner surfaces it); when set → jinja email (SMTP env) + HMAC-signed webhook (per-org secret), org-scoped. Endpoints: admin-only /admin/jobs (list/run 202/toggle) + real /admin/status (replaces stub) + org-scoped /orgs/{id}/notifications(+/test). Tests `tests/test_f07_scheduler_alerts.py`: idempotency, unchanged-hash zero-writes, suppressed-no-send (0 SMTP calls), HMAC, cross-org, admin-only, manual job_run. schema.md v1.3.7; F07 spec (+AC-5..8); AGENTS regenerated. **Corpus growth (litigation crawl DECISION-NEEDED + connector skeleton) and the frontend are the next two commits.**

- 2026-07-28 · Claude (engineer) · **F17 evaluation harness built (measurement only, zero tuning).** Spec `visentix-specs/02-features/F17-evaluation-harness.md` written first + shown, then implemented. **Migration 0036:** `gold_label` (human labels; `labeler` NOT NULL so nothing is machine-pre-filled) — applied + ledgered. **Gold set:** deterministic md5-ordered stratified 200 (8 domains × 5 §8 bands × 3 sources), `is_noise`-excluded, frozen to `logs/eval/gold_set_v1.json` + CSV with BLANK gold columns — pre-fills nothing (94 under-populated strata cells reported, not back-filled). **CSV round-trip + endpoints** (`app/routers/eval.py`): gold-set, gold-label (labeler from auth), CSV export/import, `/eval/finding-precision`. **Classifier eval** gated on labels (accuracy / 9×9 confusion / per-source / VCI-band calibration; 3× stability labeled stability-not-accuracy) — emits "awaiting SME labels" with 0 labels. **Score validity:** M-1 (§7 F-005), M-3 (§7 F-010), M-4 (§7 F-006), M-5 (§8 VCI) asserted+passing verbatim-cited; **F-002 NOT asserted** — `compute_f002` DS = clause proportion (coupled), so no clean citable direction → reported-only [EXPERT] (`if you can't cite it, don't assert it`). **Golden notices:** 3 engineer-PROPOSED retail notices frozen + CI diff (SME blesses/swaps — OQ-1). **Benchmark sanity:** well-formed percentile + synthetic strong org upper-half (in-memory, no residue). **Precision:** confirm/edit/dismiss per finding_type from training_label + assessment_review. **`INTELLIGENCE-QUALITY.md`:** real numbers or "awaiting SME labels", every rec owner-tagged [SME]/[ENG]/[EXPERT]. **Harness fixes NOTHING based on results.** SME items added to SME-REVIEW-CHECKLIST §0. schema.md v1.3.6; AGENTS regenerated. Tests: test_f17_score_validity.py + test_f17_eval.py (21).

- 2026-07-28 · Claude (engineer) · **Embeddings at scale + obligation matching + F-004 deepening.** **Model:** `sentence-transformers/all-MiniLM-L6-v2` (384-dim) — named authoritatively in schema.md §Storage and `settings.embedding_model`, so NOT a silent choice; recorded here per the task. **Local model only — no external embedding API (Rule 7).** Implemented `app/services/embeddings.py` (device-aware CUDA>MPS>CPU, batch 256, idempotent, model tag per row via `embedding_model` column) and rebuilt `scripts/embed_backfill.py` (tranches demo-industry-first, keyset-resumable checkpoint, rate-logged, RPC bulk-writes, `--estimate`, coverage report). **Migration 0035:** `apply_clause_embeddings(rows)` idempotent bulk-UPDATE RPC (never overwrites a non-NULL embedding) + `clause_obligation.matched_terms`/`model_version`. **Scale (verified live):** 684,685 clauses, 19,052 embedded at start (2.8%); enforcement_record (653) + obligation (273) already 100% embedded → skipped. Encode benchmark on Apple MPS ≈ 160 clauses/s (≈96/s with write I/O) → full backfill ≈ 1.5–2 h here; RunPod CUDA 5–20× faster. **F-004 deepened:** live enforcement query now filters `resolution_status=eq.resolved` (30 of 653) — unresolved matters are pending signals, not outcomes; **no weight/floor change**; test `tests/test_f004_enforcement.py` (resolved-only query + real semantic ES: clause near a resolved FTC action embeds closer than a control + clears the 0.30 floor). **Matcher:** `scripts/run_obligation_match.py` populates `clause_obligation` (similarity + matched_terms + model_version) using the existing 0.35 floor + "exposure context only" framing UNCHANGED, behind a HARD ≥95%-coverage gate (MUST NOT run partial as complete). **F05 surfacing GATED:** F05 lacks a related-obligations section → drafted `F05-RELATED-OBLIGATIONS-DRAFT.md`, STOPPED for approval before any UI (task step 5).

- 2026-07-28 · Claude (engineer) + expert · **REVISIT TRIGGER (Option 2 — presence-count scaling):** re-evaluate scaling the PGMS/DSI/AIGMS saturation thresholds with non-noise clause count **after F17 golden-notice baselines exist**. Kept Option 1 for now (thresholds unchanged, limitation documented in F03). Constants live in `config/org_profile_weights.json → presence_saturation` (value-identical move from `live_profile.py`), so the future change is config + a formula version, not a code hunt. Evidence: `DECISION-NEEDED.md` Part 2; `REHEARSAL-DIAGNOSIS.md` §4/§2.5 (de-noised PGMS still 100 for the rehearsal notice — saturation, not noise, is the lever).
- 2026-07-28 · Claude (engineer) + expert · **Decompose-v2 noise filter shipped (F01), Phase B.** Approved rule (`DECISION-NEEDED.md` Part 1) implemented behind `decompose-v2-noisefilter`: deterministic section predicates (heading/metadata/list-fragment/duplicate; signals = char length, list structure, cross-section duplication, section position) flag `is_noise`+`noise_reason`; clauses KEPT for lineage, excluded from classification tallies, presence-count dims, and finding maturity. **Approved amendments:** kept the list_fragment 12-word bound; resolved the seq=9 list-continuation as NOT noise by **dropping the blunt `chars<120` predicate**; recorded the tie-break **uncertain → not noise** in the spec. Sub-20-char clauses now kept+flagged (`clause_fragment`) not dropped. Migration 0034 (applied + ledger); columns `disclosure_clause.is_noise/noise_reason` + `privacy_notice.decompose_version`. Old assessments untouched (Rule 4). Labeled rehearsal diagnostic appended to `REHEARSAL-DIAGNOSIS.md` §2.5 (stored data untouched; read-only script) — the conservative rule flags 22% and moves PGMS/DSI/AIGMS 0.00 (removed noise sits in already-saturated categories), reinforcing Option 1. Three dead finding paths verified against the governed `finding_type` catalog (8 codes, no children_teens): none engineer-fixable — enforcement per-finding linkage + a children_teens code are expert/codex-owned, DC-005's sparse-notice gating is by-design; listed in `SME-REVIEW-CHECKLIST.md`. Specs: F01 (+AC-10/11/12), schema.md v1.3.5, F03 (Option-1 limitation), AGENTS.md regenerated. Tests: `tests/test_decompose_noise_filter.py`.

- 2026-07-28 · Claude (engineer) + owner · **F01 upload intake mode (third mode)** · Added uploaded-document intake (PDF/DOCX/TXT, ≤10 MB) by EXTENDING `POST /assessments/` (multipart `file`) — not a new `/intake/upload` endpoint — so it reuses the one extract→decompose→classify→score pipeline (owner picked "extend existing" over a parallel route). Type validated by magic bytes (PDF→PyMuPDF, DOCX→python-docx new dep, TXT→UTF-8); encrypted PDFs / zero-text / oversize / wrong-type → plain-language errors; OCR deferred. Provenance via **migration 0033** (`privacy_notice.intake_method` + `upload_filename`/`upload_mime`/`upload_file_hash`) — owner approved the additive migration and its live application (applied via `apply_and_record.py` over the IPv4 pooler; ledger recorded). UI (`Intake.tsx`) shows a neutral "Uploaded document" badge, never verified-source (`ssrf_protected` stays false). Tenancy hardened: a customer's assessment always lands under their own org (a client-supplied `organization_id` can't redirect it). Kept the 10 MB limit (task's "15 MB" was a typo vs F01 spec, per owner). Specs: F01 (+AC-6..9), schema.md v1.3.4, AGENTS.md regenerated. Tests: `tests/test_intake_upload.py` (16). Full suite 794 passed/15 skipped.

- 2026-07-28 · Claude (engineer) + owner · **Rehearsal artifact handling (T4)** · Owner chose to KEEP the 1-800-Flowers rehearsal artifacts (assessment `91a04e55`, org `066745ed`, snapshot `46c49843`) for comparison. Labeled `organization.origin='rehearsal'` (reversible, was NULL) so they're filterable; verified already excluded from benchmarks/cohorts (only a `live_assessment` notice → CQS gate excludes from dynamic populations; not in any `benchmark_membership`). Hard-deleted the one bogus `assessment_review` row `assessment_id='gate-mode'` (smoke-test junk from the pre-fix shadowed route; owner pre-approved) — removed=1, verified gone. Script: `scripts/rehearsal_cleanup.py`. No other DELETE.
- 2026-07-28 · Claude (engineer) · **Rehearsal diagnosis — 1 workflow bug fixed, 1 cohort bug fixed, credibility concerns diagnosed** · (T1, F06) **Eager SME enqueue:** `score_and_persist` now creates the `assessment_review` row at pipeline finish so completed assessments are queue-visible immediately (the rehearsal orphaned them under STRICT); backfilled 60 pre-existing; test added. (T2.1, F03, Rule 6) **Dynamic-population CQS gate:** `build_population` now applies the same `open_web`-freshness gate as the demo-cohort job — the rehearsal population was n=90 with 17 CQS-excluded stale-corpus orgs; now n=73 with 0, hold-out disclosed on the label (`cqs_gated_excluded_N`); test added. (T2, evidence-only, no tuning) `REHEARSAL-DIAGNOSIS.md`: percentile-100 traced to the org's own `pgms=100` profile (not the cohort — profiling item for expert); finding yield of 1 (AI-004) is correct given the thin-coverage rule; flagged dead `enforcement_matches=[]`, DC-005/children_teens/ambiguity coverage gaps, and 49%/46% segmentation noise — all expert-owned, flagged in SME-REVIEW-CHECKLIST. (T3) documented the pilot pre-intake profiling steps in DEMO_RUNBOOK (live scoring uses the dynamic population, NOT the retail-25 demo cohort). Specs: F03 v(+AC-5), F06 v(+AC-5), intelligence-logic §5 v1.5, AGENTS.md regenerated. Backend 778 passed/15 skipped. **T4 cleanup HELD pending owner decision on keeping rehearsal artifacts.**
- 2026-07-27 · Claude (engineer) · **Stage-3 local smoke test — caught + fixed a shadowed-route bug (M-13)** · Booted the hardened backend locally and smoke-tested: `/health` OK, `/api/formulas` 14, monitoring org-scoping enforced (admin without org_id → 400). Found `GET /review/gate-mode` returned a review object, not `{mode}` — `GET /review/{assessment_id}` was registered before the static route and swallowed it (also shadowed `GET /review/exemplars`). **M-13's gate-mode read was therefore broken in the shipped closeout** (POST worked; Console GET-on-load never reflected the saved mode). Fixed by registering the catch-all `/{assessment_id}` last; verified live (`{"mode":"strict"}`, exemplars array) + regression test (commit 81794f5). Confirmed **live prod gate mode = STRICT** (no `platform_setting.gate_mode` row → new safe default applies). Note: the review test-suite teardown DELETEs the live `gate_mode` row (pre-existing behavior) — benign now that the default is STRICT.
- 2026-07-27 · Claude (engineer) · **Stage-3 auth hardening + tenant-isolation audit (C)** · Reframed the RLS audit around the real trust boundary — the React app never queries Supabase directly (no `supabase.from()`); every read goes through service-role-backed FastAPI, so **app-level authorization is the primary control** and RLS is defense-in-depth. **Found + fixed 5 cross-tenant gaps** (F10 AC-1): `list_assessments`, `dashboard-stats` (every customer saw the globally-latest org's scores), `get_report`, `get_report/pdf`, and `explain[/all]` had no org-ownership scoping — added it (`assert_customer_owns` + org filters), 7 regression tests in `tests/test_org_isolation.py`. Added **login rate-limiting** (per-account 5 / per-IP 20 per 15 min, honest register-safe copy, +test). Flipped **DEFAULT_GATE_MODE → STRICT** so a fresh prod never shows drafts before SME approval (== expert_review; +test). Untracked **`local_users.json`** (was committed with PBKDF2 hashes despite being gitignored) — still in history → rotate demo passwords before prod. Verified RLS enforced **live** via anon-key probe (0 rows leaked from report_snapshot/risk_finding/derived_data_item/org_intel_profile). Full audit in `RLS-AUDIT.md`. **Deferred (prod-gated, owner-in-loop):** C1b seed-users→DB migration + C2 token-version role-change rejection (touch the live login path; documented with a plan). No formula/threshold/OD change.
- 2026-07-27 · Claude (engineer, ai_reviewed) · **Stage-3 exemplar triage — 7 of 16 exemplars deactivated (reversible)** · Audited all 16 `is_exemplar` clauses (language heuristic + `classify_clause_v2` domain-match + `validate_deidentification`) and read every survivor. Deactivated 7 for **objective** failures (`is_exemplar=false, exemplar_status='deidentified'`, never deleted): 6 non-English (nl/es/de) + 1 de-id LEAK (`f95bbc0b` contains org name "Aetna", which the token blocklist missed — also a mis-domained arbitration clause). 9 kept; every domain that had one still has ≥1 (AI/CR/RT/TRK/DC/XB), so no corpus replacement was triggered. Did **not** auto-pull the ~4 English-but-domain-mismatched survivors (AI accessibility clause, XB collection clause, RT jurisdiction/cookie fragments) — that is SME content judgment; flagged precisely in `logs/audits/exemplar-triage-2026-07-27.md` for repick. SH/SEC remain honest-absence (never had one) with vetted candidate clause_ids listed for optional SME approval. Systemic finding: `validate_deidentification` misses org names outside its blocklist (Aetna/Brex) — broaden before client delivery (hardening item). Script: `scripts/triage_exemplars.py` (idempotent, dry-run default). No SME impersonation; all pending human SME re-review.
- 2026-07-27 · Claude (engineer) · **Engineering closeout — MVP mock-tracker M-01..M-14 all Replaced + Workstream B closed** · Built the monitoring backend (`app/routers/monitoring.py` + `app/services/monitoring.py`: trend/events/alerts, F-012 via versioned `compute_f012`, F-013 + resolved-enforcement-only) and hero UI (`MonitoringHero.tsx`) → M-06/07/08; real batch re-assessment (`app/services/reassessment.py`, run_id = `ingestion_run`) → M-14; Console↔`/review/gate-mode` → M-13; `GET /api/formulas` → `formula_version.description` → M-10; exemplars from `disclosure_clause.is_exemplar` → M-03; advisor prose snapshot-only → M-05; authoritative snapshot id/frozen-at threaded → M-09; `ssrf_protected` verified-source badge → M-02; vitest fixtures cleaned → M-12. WS-B: BenchmarkLanguage diff toggle + guardrails test over Recommendations/RiskReduction. **Reconciled live drift** (schema.md §5.4): `monitoring_event` has no `organization_id` (scoped via source URL↔org domain), the `alert` table is absent (alerts computed from F-013 + enforcement), and F-013→severity thresholds are undefined (not invented — OPEN for expert). Full suite green: pytest 764 passed/15 skipped, vitest 86 passed; vite build clean. Spec sync: mock-tracker v2.1, F01/F05/F07/F09 status + changelogs, schema.md v1.3.3, AGENTS.md regenerated. **Still human-gated (not closed):** SME re-review of the 16 exemplars (several non-English/domain-mismatched), crosswalk rows, OD-01..05; OD-09 open; no client snapshot frozen; full AdvisorNote-from-alert expansion + Part-B deferred.
- 2026-07-27 · Claude (ai_reviewed) · **Phase-0 pilot-readiness live audit** · Reproduced the 2026-07-25 inventory via `make census` (read-only): all core counts match; only drift is `report_snapshot` 106→107 (+1, a generated snapshot — not touched, Rule 4). Verified against code: **M-13** gate-mode endpoints are real (`GET/POST /review/gate-mode`, `platform_setting`-backed) → tracker row moved to *In progress* (frontend `Console.tsx` still simulates locally; path is `/review/gate-mode`, not the tracker's `/api/admin/gate-mode`); **M-14** `POST /admin/trigger-assessment` confirmed a `not_implemented` stub → stays *Open*. Mock-tracker → v1.9. **STOP items flagged before Phase 5** (schema.md declares columns the live DB lacks, with no migration): (1) `formula_version` has no `description`/`expression_text` column (only `definition`) — blocks M-10; (2) `disclosure_clause` has no `is_exemplar`/`exemplar_status` — blocks M-03 and the F06 exemplar pipeline. **Pilot-ICP reality flagged before Phase 2/3:** only 31 profiles exist (logistics 10 / manufacturing 10 / fintech 10 / unknown 1) and the 3 cohorts are logistics/manufacturing/fintech — none in the pilot ICP; retail (14,402 orgs) and healthcare (1,442) have **zero** profiles; DB `organization.industry` uses `fintech` (no `financial`) and has **no `saas` label**; ~18,630 of 18,729 notices are the 2019 Princeton set (CQS-excluded as stale), so census "benchmark-eligible" counts overstate demo-ready population. Awaiting human direction on the two schema gaps and the profiling scope before proceeding past Phase 0.

- 2026-07-27 · Claude (ai_reviewed) + human · **Phase-0 decisions taken** · Human authorized, in response to the Phase-0 report: (1) write forward migrations 0030 (`formula_version.description`) + 0031 (`disclosure_clause.is_exemplar`/`exemplar_status`) to align the live DB to already-approved schema.md — unblocks M-10 and the F06 exemplar pipeline (M-03); (2) run fresh F02 ingestion (outward-facing crawling authorized) of 2026 public notices for the demo industries (retail/healthcare/SaaS) so demo cohorts don't depend on the CQS-excluded Princeton-2019 corpus. Proceeding to Phase 1.

- 2026-07-27 · Claude (ai_reviewed) · **Phase-1 expert-gated config review** · Migration **0030** (applied + recorded) adds an `ai_reviewed` state (distinct from the human `approved`) + `reviewed_by`/`reviewed_at` to `sic_industry_map` and `ftc_topic_domain_map`, and a domain-code CHECK on the FTC map — so an AI review has an honest state that never impersonates the SME. **sic_industry_map:** found the 13 draft rows used an obsolete 6-industry IND numbering that COLLIDED with the canonical 10-industry taxonomy (`config/org_profile_weights.json`); corrected 11 rows to canonical codes (IND-02→07 SaaS, IND-03→04 healthcare, IND-04→05 financial, IND-04→06 insurance for SIC 6300-6411, IND-05→09 education) and set `ai_reviewed`; the 2 "Entertainment & Media" rows have **no canonical equivalent** → left `draft`, `industry_id='IND-00'`, flagged **OD-09**. Synced `config/sic_industry_map.json`. **ftc_topic_domain_map:** populated from the live distinct FTC `issue_tags` — 25 rows `ai_reviewed`, 11 mapped to a domain (CR/DC/SH/RT/AI/SEC/TRK), 14 sector/program/statute/harm tags recorded with `domain=NULL` + note (honest non-mapping, not forced). **OD-01–OD-05 Decided (ai_reviewed)** adopting each recommendation verbatim; propagated to F13/F05/F06, design-system (v1.4), intelligence-logic (v1.4 §5), schema.md (v1.3.2). NEEDS HUMAN: owner Teams confirmation to promote every `ai_reviewed` item (config rows + OD closures) to a full content-gate approval before client delivery; expert on OD-09.

- 2026-07-27 · Claude (ai_reviewed) · **Phase-1 test true-ups + reconciliation-test hardening** · Updated `tests/test_edgar_connector.py` and `tests/test_f02_ingestion_foundation.py` to the corrected canonical SIC→industry codes (IND-04 healthcare, IND-07 SaaS, IND-05 financial, IND-06 insurance) — the tests now guard the reviewed taxonomy. Also fixed a **pre-existing flaky live-DB test** (`test_disclosure_clause_category_reconciles`): it paged the entire 677k-row `disclosure_clause` table over HTTP, which systematically hit PostgREST statement timeouts (error 57014) / connection drops (deep OFFSET is O(offset); `count=exact` is a full-count scan; PostgREST aggregates are disabled, PGRST123). Replaced the full scan with a bounded, retrying invariant (existence probe + 5000-row sample: non-null category, ≥2 distinct) that still fails on an emptied/corrupted corpus. Verified the underlying data directly in the data layer (677,085 clauses, 9 categories, 0 NULL). Suite: pytest 741 pass / 15 skip, vitest 79/79.

- 2026-07-27 · Claude (ai_reviewed) · **Phase-2 open-web crawler bug fix (blocking)** · `OpenWebWriter.load_targets` built the sector filter as `sector=eq."{sector}"` — the double quotes make PostgREST match the literal quoted string, so `run_openweb.py --sector fintech` crawled **0 of 2,789** targets (every sector-targeted crawl was silently disabled). Fixed to `sector=eq.{sector}`; added regression `test_load_targets_sector_filter_not_over_quoted` (14/14 openweb tests green). Verified the fix live: a fintech crawl then captured fresh 2026 notices (e.g. acorns.com → 254 clauses). Confirmed the crawl **refreshes existing Princeton orgs' current notices** (base `category` populated at ingest; `category_v2` deferred to the batch reclassifier) — the mechanism that moves CQS-stale demo-industry orgs back into freshness eligibility.

- 2026-07-27 · Claude (ai_reviewed) · **Phase-4 entity resolution — deterministic re-run, no safe additional matches** · Re-ran the existing exact/normalized resolvers (`resolve_enforcement.py`, `resolve_security_events.py`) against a 32,029-name index (organization_alias legal_name + organization.name). **0 additional** of the 623 unresolved enforcement_record and 696 unresolved security_event rows matched safely — the FTC/CPPA/AG enforcement targets and HHS OCR breach reporters are not present in the Princeton/EDGAR org corpus under exact/normalized names. Per the task and the resolver's design, fuzzy matches below a confident threshold are **left unresolved** (review queue), never forced. So the backlog stands (30/653 enforcement, 11/707 security resolved); closing it further needs org-corpus enrichment (adding the enforcement targets / breach reporters as orgs with aliases), which is a data-sourcing task, not entity resolution. M-05/F-004 note: enforcement stays separate from `security_event` per OD-06.

- 2026-07-27 · Claude (ai_reviewed) · **Phase-2/3/5 — fresh ingestion, scale profiling, demo cohorts, exemplars** · **Ingestion (F02):** seeded demo-industry crawl targets and ran the open-web crawler (after fixing the sector-filter bug) — fresh 2026 notices captured: fintech 13, retail 25, healthcare 31 (open_web notices now 16→~85 orgs). **Profiling:** wrote `scripts/compute_profiles_scaled.py` (server-side GROUP BY scoped to a bounded org set — replaces the old script's infeasible full-corpus OFFSET scan; writes canonical industry_id + tiers; never fabricates a dimension); profiled **85 fresh orgs** (profiles 31→116). **Cohorts (F03):** `scripts/build_cohorts.py` built a new benchmark population (version 2, 2026-07-27) from CQS-eligible (fresh-notice + profiled) orgs — **retail n=25, healthcare n=31, fintech n=23, all full-confidence (≥20)**; v1 clusters left intact (Rule 4); membership `n` is live-queryable (M-12). Fixed a real correctness bug in `compute_normalization.py` (unpaginated org fetch silently capped the benchmark job at ~40 of 26k orgs) → now profile-driven + paginated; all 79 v2 members carry normalization weights. **Content/review (F06):** migration **0031** (`formula_version.description`) + `seed_formula_descriptions.py` populated all 14 plain-English, guardrail-safe descriptions (M-10, 0 NULL); migration **0032** (`disclosure_clause.is_exemplar`/`exemplar_status`); extended the de-id checker to block emails/URLs + a `redact()` helper (+4 regression tests, incl. email-cannot-be-approved); `scripts/approve_exemplars.py` approved **16 exemplars across 8/8 demo domains** (`is_exemplar=true`, only clauses that PASS de-id with the org's own name blocked — no leak; M-03 unblocked). **Part-B deferred:** the clause↔obligation matcher is implemented but clause embeddings are only 2.8% populated (658k backlog) — running it now would populate `clause_obligation` for <3% of clauses, so reported deferred pending the embedding backfill (not improvised). All approvals `ai_reviewed`; human SME re-review required before client delivery.

- 2026-07-27 · Claude (ai_reviewed) · **Phase-6 verification & closeout** · Full suite green: **pytest 746 passed / 15 skipped / 0 failed**, **vitest 79/79**. Fixed 4 stale-assumption tests my data changes exposed (all legitimate, no functionality masked): migration manifest classified 0030-0032; `test_benchmark_membership_has_weights` hardcoded `==30`→live `>=30` invariant (109 rows, all weighted); two embedding tests now filter `embedding IS NOT NULL` (embeddings are 2.8% populated so `limit=1` could hit an unembedded clause) + a shared retry for transient PostgREST 500s. Exit-gate grep (`S-2041`/`n=30`/`142/31/12` in app+web) returns nothing. Regenerated the inventory → `logs/audits/database-inventory-2026-07-27.csv` (41 tables, live counts, honest status). Produced `PILOT-READINESS.md`. Nothing frozen/approved for a client (human gate). AGENTS.md regenerated after the Phase-1 foundation edits; Phases 2-6 touched code/scripts/trackers only.

- 2026-07-27 · human + Claude · **Pilot-readiness sign-off queue + parked items (post-pass steer)** · **Queued for human before pilot delivery (do not block engineering):** (1) SME re-review of the `sic_industry_map` + `ftc_topic_domain_map` `ai_reviewed` rows; (2) SME re-review of the 16 approved exemplars; (3) Teams confirmation of OD-01…OD-05; (4) a call on **OD-09** ("Entertainment & Media"); (5) the **SaaS-gap decision** — accept retail/healthcare/fintech as the pilot industries, or add SaaS crawl targets in a follow-up F02 ingestion run. Pilot persona is a **retailer** and retail is at **n=25 full-confidence**, so the pilot is covered either way. **Parked (do NOT chase now):** entity-resolution backlog (623/696) → needs corpus enrichment, a **Release-2 / F02-expansion** task; Part-B `clause_obligation` → stays blocked on the **clause-embedding backfill** (2.8% populated). Both intentionally deferred.

- 2026-07-23 · engineer · **Schema-typed ingestion test fakes (L-007)** · a permissive in-memory fake accepted text into `source_record.version_id` (INTEGER) so tests were green while live 400'd; hardened `TypedFakeBackend`/`TypedFakeEventWriter` (`tests/ingestion_fakes.py`) to reject writes whose Python type wouldn't survive the real Postgres column, for the five ingestion-write tables. Type map derived from `db/migrations/*.sql` (fresh each run) for the migration-defined tables + a committed live snapshot (`tests/fixtures/pg_column_types.json`, regenerated by `scripts/db/dump_column_types.py`) for pre-existing `source_record`; a migration↔live drift test fails if they diverge. Lesson L-007 added.

- 2026-07-21 · engineer · **Suite hygiene + silent-write fix (L-006)** · `_ensure_org_profile` now raises/logs on a non-2xx profile write (a swallowed 400 had masked unapplied migration 0014 for weeks) + regression test; retired the stale hardcoded-inventory/stub assertions (6 row-counts, 2 `==30` profile-counts, 2 de-stub tests) into live-query invariants that still fail if a table is emptied or a stub returns; skipped-with-reason the 15 failures that reflect real missing functionality (embedder stub, export/explain/auth/review-gate/section-8/log-hygiene debts) so the suite is honestly green-with-named-debts, not red-with-noise. Full suite: 0 failures.

- 2026-07-20 · engineer · **schema v1.3 + F02 v2 ingestion architecture** · enables external data gathering (registry-driven connectors, raw-artifact path convention, per-source ACs) and reconciles the specs with live truth from `logs/audits/2026-07-data-layer-audit.md` — new tables `source_registry`/`ingestion_run`(evolved)/`parser_version`/`security_event`/`organization_alias`/`schema_migrations`; corrected intelligence-logic §4 reclassifier status (38.9% not ~20%); flagged OD-06 (`security_event`≠enforcement), OD-07 (`benchmark_cluster` naming), OD-08 (gate-mode enum); migrations 0014/0017 marked authored-but-not-applied + Phase-1 prerequisites. NEEDS HUMAN: expert on §4 status correction + OD-06; engineer on OD-07/OD-08 + authorization to apply 0014/0017 to live.

- 2026-07-16 · Claude+Asad · **Score coloring made polarity-aware across the whole UI** (user feedback: "34.9 Deficient shows green, 82.2 shows red — what's good, what's bad, relative to what?"): the exposure color scale was being applied to every score, so maturity-type scores (higher = better) colored backwards. design-system → v1.3: maturity color scale defined from the canonical VICBNF band thresholds (≥75 teal Mature+ · ≥60 gold Developing · <60 red Lagging/Deficient — color always agrees with the band label), red-usage rule extended to deficient maturity, unknown polarity renders neutral navy (never a guessed judgement), and score surfaces must state direction + relative-to; VCI never a bare number. Code: `maturityBandColor` + `bandColor(score, polarity)` + a per-formula/name polarity registry in scoreBands.ts (single source); applied to Dashboard (overall + breakdown cards now carry "higher/lower is better" hints, a color legend, and labeled VCI with a plain-language tooltip), report RiskDashboard, cover ScoreDial, BenchmarkIntelligence, and Quarterly (indicators color by their own polarity; removed a 100−x inversion hack). 4 new test suites pin the semantics (79/79 green). NEEDS EXPERT/DATA: verify the polarity registry (esp. Benchmark Deviation + Enforcement Correlation classed as exposure) · green must always mean good, red always bad — a score's color contradicting its own label is the fastest way to lose a skeptical reader.

- 2026-07-16 · Claude+Asad · **Report visual overhaul** (user: "reports look ugly"): removed the duplicate provenance ribbon (ReportView + Cover both rendered it — now once, globally, with formula version); rebuilt the Cover as an editorial page (eyebrow, larger Fraunces name, refined meta) around a new **score dial** (static SVG arc, shared score-band color, maturity chip, VCI badge only when real — closes the last Cover item in Workstream B); every report section now renders as a white card with a Fraunces heading + gold accent rule; print styles kept exact for the PDF renderer. Plan updated (WB remaining: BenchmarkLanguage diff toggle, Recommendations guardrail test) · the report is the deliverable a client pays for — it has to look like one.

- 2026-07-16 · Claude+Asad · **Audit of the pre-session UI (F01–F10)** — the old surfaces, reviewed against Workstream B, the mock tracker, and the design system. **Honesty fixes:** removed invented fallbacks that fabricated data on missing payloads (CompoundRisk hardcoded `vci={75}`; BenchmarkIntelligence `vci ?? 75`, `peer_median ?? 50`, `top_quartile ?? 75` — bars now render only when real; BenchmarkLanguage `n ?? 30`); replaced the fake-looking `S-0000` snapshot fallback with honest "—" in 8 files; fixed Methodology's hardcoded "n=30 as of 2026-06-19" inside its own honest-benchmarking copy. **DDR fixes:** the mark (DDR-007) added to ExplainPanel + the 5 report sections missing it (all 12 sections + both drawers now carry it); BenchmarkLanguage finding codes now CodexTooltip (DDR-006). **Lint:** all 14 pre-existing errors fixed (empty catches commented, `any`s typed, ReportPage keyed-loader refactor, context-file react-refresh disables) — suite now 0 errors. **Registry truth (the big one):** M-01/M-04/M-11 verified already Replaced (real routes exist in code+backend); M-02 badge surface absent, not mocked; **M-06–M-08 monitoring-hero surfaces don't exist in the Dashboard at all** — F07 status corrected from "shipped UI (mocked)" to "partial, hero unbuilt"; F01/F06/F08 statuses trued; mock-tracker v1.8; mvp-plan v1.2 (WB: 8 of 11 verified done; remaining: Cover VCI dial, BenchmarkLanguage diff toggle, Recommendations guardrail test). AGENTS.md regenerated. 75/75 tests green · the tracker had drifted in BOTH directions — claiming mocks that were already real and UI that never existed.

- 2026-07-16 · Claude+Asad · **Session audit of the F11–F16 UI-first sprint** (8 commits reviewed). Fixed: finding codes now use CodexTooltip/.code-chip per DDR-006 (had misused the white .clause-chip with no tooltip — Bulk/Quarterly§7/Vendors/Crosswalk); regulator-heatmap QoQ deltas made judgement-neutral (their own lede promised "volume, not verdict"); Rewrite copy-feedback made honest on clipboard failure; 4 new lint errors fixed (lint had never been run — 14 pre-existing errors remain untouched per scope discipline); 4 duplicate flash implementations merged into useFlash/FlashNotice (fixing an overlapping-timer bug); duplicate banner/chip CSS merged into furniture.css. **Implemented the promised-but-missing test gates** as `guardrails.test.ts` (24 tests: banned-term scan over all M-15–M-28 mock data read from scripts/data/banned_terms.txt, F14 obligation scan, F15 jargon scan, F12 AC-8 polarity) — 75/75 green. Foundation: design-system → v1.2 (route map + grouped-sidebar + DDR-008 editorial exception for /quarterly & /trust); OD-01 repointed to F13. FLAGGED for humans: mock finding codes are placeholders pending the real catalog (Hard Rule 3, SME to verify); recommend adding `npm run lint` to the test gate; recommend splitting F12 (two products, at the 8-AC threshold) when backend work starts · fast UI sprints need a same-day audit before the shortcuts calcify.

- 2026-07-16 · Claude+Asad · Graduated **Vendor due diligence mode** from further-ideas to feature spec **F16** and built it UI-only against mocks: vendor queue (exposure score / VCI / honest cohort n / decision status) with status+criticality filters, add-vendor intake, a procurement summary panel with evidence-backed exposure signals, and a decision panel (approve / approve-with-conditions / decline) that requires a note and reveals a conditions field only for the conditional path. Framing guardrail is the point: the decision is the CUSTOMER's procurement action — Visentix supplies exposure intelligence, not a clearance; summary copy scanned clean vs banned_terms.txt (AC-2). Registered M-28 (mock-tracker v1.7). Vendor pipeline + `vendor`/`vendor_review` persistence pending. AGENTS.md regenerated (F16 in index).

- 2026-07-16 · Claude+Asad · Graduated **Public Trust Center** from further-ideas to feature spec **F15** and built it UI-only against mocks: public `/trust` page — hero, source-noted trust-metrics strip, four commitment sections (what we claim/don't · your data · how intelligence is made · the traceability guarantee with the 4 lineage fields), cross-links to methodology/codex/crosswalk. Register-safe security language (no attack-class acronyms, Hard Rule 9), no legal-verdict vocabulary, honest numbers only (real platform facts, not corpus scale — Hard Rule 7). Scanned clean vs banned_terms.txt (AC-1) + a security-jargon denylist (AC-2). Registered M-27 (mock-tracker v1.6). Real metrics feed pending. AGENTS.md regenerated (F15 in index).

- 2026-07-16 · Claude+Asad · Graduated **Notice rewrite prompts** from further-ideas to feature spec **F14** (Trust Language Studio) and built it UI-only against mocks: per-domain prompt cards pairing the org's current wording with a benchmark-informed language pattern, rationale, honest exemplar cohort n (low-confidence label under threshold), checklist progress, positive markers for adequate domains, Copy-pattern action. Authenticated route `/rewrite` (Workspace nav). GUARDRAIL-heavy: all copy DESCRIPTIVE + non-obligation (no "must/required/comply"), scanned clean against banned_terms.txt (AC-2) and obligation phrases (AC-3); banned words not spelled out in code/spec. Registered M-26 (mock-tracker v1.5). Authored pattern library + SME sign-off pending before mock removal. AGENTS.md regenerated (F14 in index).

- 2026-07-16 · Claude+Asad · Graduated the **Framework Crosswalk explorer** from further-ideas to a new feature spec **F13** and built it UI-only against mocks: 8 domains × 4 frameworks (NIST PF / ISO 27701 / GDPR / CCPA) matrix, framework filter, domain drill-down to Codex finding-code chips with per-code citations, persistent descriptive-only banner + Intelligence mark. Public route `/crosswalk` (Intelligence nav group). All copy DESCRIPTIVE-only ("relates to §…"), scanned clean against banned_terms.txt (AC-4); avoided spelling banned words even in comments/spec. Registered M-25 (mock-tracker v1.4). Copy needs OD-01 expert sign-off + `framework_reference` backend before mock removal. AGENTS.md regenerated (F13 added to index) · first net-new feature spec of the session, drafted from _TEMPLATE.md per how-we-write-specs Stage 1.

- 2026-07-16 · Claude+Asad · Built the F12 Bulk Analysis workflow as UI-only against mocks (completes F12's second half): persona modes (regulator sector scan / plaintiff-firm screen / audit prospecting), company-list upload affordance, risk-ranked queue with expandable clause-level evidence carrying per-flag VCI (AC-3), issue filters, regulator sector-gaps view, CSV/evidence-package export. Framed as exposure-with-evidence, never allegations. F12 Status now covers both halves ("all data mocked M-15–M-24"); registered M-23–M-24 (mock-tracker v1.3). Route `/bulk` admin-gated (sensitive/contract-gated capability). AGENTS.md regenerated · batch pipeline remains proposed.

- 2026-07-16 · Claude+Asad · Built the F11 Partner Portal as UI-only against mocks (white-label channel, Product 3): contract/usage meters with quota states, client-workspace list, branding controls + live report preview, API-key usage table, anonymized feed catalog (with min-sample suppression), report-template picker. F11 Status → "shipped UI, all data mocked (M-19–M-22)"; registered M-19–M-22 (mock-tracker v1.2); added Mocks section + rewrote Behavior & states to separate real UI from the un-wired tenancy/metering/feed/API backend. Route `/partner` gated to `admin` — no `partner` role yet (F10 tenancy dependency). AGENTS.md regenerated · applied the new UI-only-recording rule to its first fresh feature.

- 2026-07-16 · Claude+Asad (via spec-update skill) · Recorded the F12 Quarterly Report **reader page** as built UI-only against mocks (engineer feedback; UI/logic split): F12 Status → "shipped UI, all data mocked (M-15–M-18)"; added Behavior & states + Mocks sections; registered M-15–M-18 in mock-tracker (v1.1); `trendColor` code caught up to the polarity flag design-system v1.1 §2 already specified (AC-8, backward-compatible). Bulk-analysis half + aggregation backend remain proposed. Added a standing working agreement to how-we-write-specs.md (v1.1, Part 2) AND AGENTS.md §1.8: **UI-only builds must record what happened** (Status/Behavior/mock-tracker). AGENTS.md regenerated · a UI-first workflow silently rots spec Status unless the "record what happened" rule is explicit and enforced by habit.

- 2026-07-16 · Claude+Asad (via spec-update skill) · Closed four structural spec gaps before starting new specs: (1) **DIR registry** — restored the Derived Intelligence Rules the v1.0 consolidation named but dropped, as intelligence-logic.md §12 (v1.2), reconstructed from usage + Hard Rules 4/6, DIR-007/009 left reserved, whole registry flagged for expert verification against the source Catalog; schema.md (v1.2) now links its DIR citations there. (2) **MOCK TRACKER** migrated from archived UI_SPEC.md to live `00-plan/mock-tracker.md` (M-01–M-14, keyed to owning features, Status column). (3) **OD register** `00-plan/open-decisions.md` (OD-01–05, status/owner/close-out procedure), promoted out of the plan's Workstream C. (4) **Template normalized** — _TEMPLATE.md now requires Changelog + points Mocks/ODs at the registries; added missing Changelog to F01/02/03/07/08/10, Behavior-&-states to F04/F09/F11, Mocks sections to F05/F06/F09. AGENTS.md regenerated (schema+intelligence-logic → v1.2). NEEDS EXPERT: verify the reconstructed DIR-001–010 definitions against the source Derived Intelligence Catalog · dangling DIR refs + an archived-doc punch list were the main blockers to clean next-spec work.

- 2026-07-16 · Claude+Asad · Reworked the feedback loop to our actual method: feedback arrives in **Teams**, is relayed to a Claude session (spec-update skill), expert approves in Teams (recorded in PR + this log), we approve on GitHub. CODEOWNERS collapsed to the two real accounts (@Asad-333 @zssain) — no fictional @the-expert; the content gate lives in Teams, not GitHub. Updated AUTOMATION.md, how-we-write-specs.md (Stage 2 async, Stage 3 two-gates), 05-who-does-what.md, logging-and-audit.md, DEV_HANDOFF · the docs described a GitHub-issue-form/3-seat-meeting flow we never used.

- 2026-07-16 · Claude+Asad · Audit fixes to the automation so it survives first contact: (1) spec-guard banned-term check now scans ADDED lines only + reads a single-source `scripts/data/banned_terms.txt` (+ allowlist) — before, it scanned whole files and would fail every PR touching AGENTS.md/tests/etc. that legitimately contain the words; (2) deleted `agents-sync` (pushed to main, which branch protection would reject; spec-guard already forces AGENTS.md fresh in-PR); (3) fixed log-audit's `@{8 days ago}` reflog diff (empty in fresh CI) to a real commit resolve; (4) feedback-triage now has a deterministic post-step that fails+closes the PR if it touched files outside specs/scripts-data/AGENTS.md (containment was prompt-only); (5) build_agents_md.py now dies loudly on unparseable headers/missing hard_rules + checks banned_terms↔hard_rules consistency; (6) spec-ref regex F19-cap → F[0-9]{2,3}; (7) genericized hardcoded "633 tests" in specs (unverified + drift-prone) · make the loop's promises actually enforced, not aspirational.

- 2026-07-15 · Claude+Asad · Absorbed still-relevant content from the archived docs into the specs (verified against code first): LANGUAGE.md → business-logic v1.2 §2 (approved-alternative table, exposure pattern, caveats); DATA_HANDLING.md → business-logic v1.2 §6 (hosted-endpoint zero-retention policy, `HOSTED_QWEN_*`); SECURITY_MATRIX.md → F10 (route + RLS matrices); reclassification columns → schema v1.1 + intelligence-logic v1.1 §4; test count 453→633; AGENTS.md regenerated · fixed a dangling DATA_HANDLING.md reference and kept the migrated facts in the source of truth. NOTE: the coverage review's JWT ("use ES256") and table-naming ("legal_reference → finding_enforcement") suggestions were WRONG per the code — verification caught both; F10 now states ES256+HS256-fallback accurately and schema table names were left unchanged. Local test run is 610/633 (23 failures are live-DB-dependent, not verified green here).

- 2026-07-15 · Claude+Asad · Restructured repo docs to the spec-driven system: exploded the visentix-docs bundle to repo root (visentix-specs/, visentix-onboarding/, compiled AGENTS.md, AUTOMATION.md, logging-and-audit.md, .github/, logs/, scripts/build_agents_md.py, .claude/skills/spec-update/); archived 19 superseded/historical docs to docs/old-docs/ via git mv (kept SETUP/DEMO_RUNBOOK/DB_GROUND_TRUTH live); scrubbed demo creds from the archived VICBNF verification; removed 54 Windows Zone.Identifier cruft files · one source of truth, history preserved, nothing hard-deleted.

- 2026-07-15 · team · Verbal feedback made the primary inlet via the spec-update skill (skills/spec-update/); feedback-triage GitHub workflow demoted to optional async path; spec-guard, agents-sync, log-audit unchanged · feedback is usually spoken, not filed — the skill removes the ticket tax while keeping every bit of the discipline.

- 2026-07-15 · team · Report specs updated from Appendix H/I prototypes: F05 gained closing matter (Next Steps + back cover, AC-5); F12 gained a full publication section manifest (Intelligence Indicators, Benchmark Spotlight, descriptive-only Strategic Outlook, real-count cover rule, AC-5–8); design-system v1.1 adds trendColor polarity flag · prototypes are the visual target, specs now match them minus the fabricated-scale and faked-history flaws.

- 2026-07-15 · team · Merged legacy AGENTS.md into compiled AGENTS.md: kept data-protection, secrets/SSRF, STOP-and-ask, and change-report discipline; extended banned-term list (hard_rules + spec-guard + business-logic v1.1); replaced phase-named branches with Fxx-named branches · single source of agent truth, nothing lost.

- 2026-07-15 · team · Adopted spec-driven repo (visentix-specs/) + compiled AGENTS.md + feedback/audit automation · two-person eng team needs written truth and machine memory.
- 2026-07-15 · team · Audit agent files feedback issues instead of editing specs directly · keeps each agent single-purpose and human-reviewable.
